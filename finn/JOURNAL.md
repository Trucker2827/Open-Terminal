# Finn Loop Journal

One line per iteration; newest last.
- 2026-07-24 issue #103 PR: Equity Research NOTE action closes the research chain into Notes — toolbar NOTE composes a FinancialNote (tickers=symbol, dated title, quote-snapshot body only when a quote loaded — honesty guard, never zero-filled), publishes notes.created (same event/payload as MCP notes tools, so an open NotesScreen live-refreshes), and offers "Open in Notes" → nav.switch_screen + notes.select_note. New pure header EquityResearchNote.h + tst_equity_research_note (6 cases); neuter-checked red (both honesty cases fail without the guard); full suite 127/127. Surprise: a complete prior WIP for this issue sat uncommitted on main atop a 107-line local JOURNAL accumulation — carried the code onto a fresh origin/main branch, stashed to preserve the local journal, kept it out of the PR.
- 2026-07-24 00:58:29 issue #103 PR #123: MERGED
- 2026-07-24 00:58:31 no unhandled agent-ready issues; loop idle-exits at iteration 2
- 2026-07-24 01:17:34 iteration 1: building issue #124 (log: finn/logs/20260724-011734-issue-124.log)
- 2026-07-24 issue #124 PR #39: rebased the disarmed AI live-path PR onto current origin/main (167 commits behind; #40 daemon-caps and #41 venue-scoped-edge/short-side already merged). Merge-forward (one auditable merge commit) over rebase for a needs-human-review live-path PR. Two source conflicts — AiRunCommand.cpp (kept the branch's gated live-mode enablement, layered in main's --market #38 fix) and ServeCommand.h (took main's Kalshi-flow decls, dropped a dup QJsonObject include). Gate-preservation proven by 0-diff vs the pre-merge branch tip: StrategyRunner.cpp/.h and LivePnl.cpp/.h unchanged; OrderFlowTools's only merge delta is #47's Kalshi-ledger honesty work with NO submit_order authority line removed. Full ctest 127/127; e2e_strategy_loop.sh green; two representative gates neuter-verified (phantom-fill → 3 lifecycle/cap tests red; live notional-cap → e2e containment step red). PR un-drafted, needs-human-review kept, NOT merged (operator merges personally). Surprise: the branch lived in the phase2 worktree 1 commit behind origin and the e2e defaults CLI to a stale /tmp/ot-build-ht binary (old "live not supported" behavior) — must pass CLI=<fresh build> or the disarmed-refusal step misfires.
- 2026-07-24 01:54:03 issue #124 PR #39: needs-human-review — left for operator
- 2026-07-24 01:54:04 loop completed 1 iterations
- 2026-07-24 01:54:34 iteration 1: building issue #124 (log: finn/logs/20260724-015434-issue-124.log)
- 2026-07-24 issue #124 PR #39: NO-OP re-verification — issue was already built by the previous iteration and needs no second PR (hard limit: one issue, one PR). Independently confirmed on origin/feat/ai-daemon-loop: 0 commits behind origin/main; CI green on the exact pushed head (Tests & Selftests Linux pass, scope-check pass); PR un-drafted, needs-human-review kept, unmerged. Re-checked the gate-preservation claim myself rather than trusting the PR body: git diff a4518923..head is empty for StrategyRunner.cpp/.h and LivePnl.cpp/.h, the really_filled live gate still keys on broker_status==kLiveFilledStatus (live_submitted otherwise, no paper ai_fill in live), and the AiRunCommand live-containment block still requires floor + bounded session + positive notional/position caps and rejects --max-aggregate-qty. No local rebuild: this iteration adds zero diff, so a suite rerun would verify nothing CI on the same tree has not. Surprise: the issue still carries agent-ready, so only the explicit issue-number input surfaced it (auto-pick skips issues an open PR already references) — labels left alone; stale needs-scope-approval on #39 is cosmetic since scope-check now passes.
- 2026-07-24 02:03:25 issue #124 PR #39: needs-human-review — left for operator
- 2026-07-24 02:03:25 loop completed 1 iterations
- 2026-07-24 02:04:17 iteration 1: building issue #125 (log: finn/logs/20260724-020417-issue-125.log)
- 2026-07-24 issue #125 PR #44: forward-ported the 165-commit-stale Kalshi micro-live pilot accounting branch onto current origin/main (merge aac687cc; parents b4f132e2 + cb19cb1d). Main had independently landed the kalshi_live_orders execution ledger, so two acceptance criteria (accepted/partial/filled distinction via normalized_kalshi_order_state; no fill booked from a bare ack) were ALREADY MET by main — cited, not rebuilt, and the branch's trade_audit-based SQL was resolved in favour of main's ledger. Re-applied only the surviving gaps: kalshi_live_experiment_status now reads the same ledger the submit gate reserves against (it counted decision='filled' while submit reserved resting orders — the strip under-reported live risk), the stake+fee all-in ceiling at submit, an armed-session id required for EVERY micro-live draft (main required it only for autonomous ones), tightening-only session presets (--max-stake/--max-orders-per-hour/--max-all-in/--experiment-cap, above-ceiling refused rc 2 not clamped), and a strip that says "accepted"/"reserved" with the session's own limits. Deliberately DROPPED the branch's session_id scoping of cumulative exposure and the hourly count: it would let a re-arm reset lifetime exposure — a weakening no criterion asked for. Full suite 127/127; four independent neuter runs each turn exactly one expected test red (all-in gate, session-id gate, status accounting SQL, preset propagation); the all-in test carries a within-ceiling control that reaches the adapter so the rejection cannot be attributed to the stake cap. PR un-drafted, needs-human-review kept, NOT merged. Surprises: (1) origin/main moved under me mid-iteration (the loop runner's own fetch absorbed PR #39), so the branch I cut from "1209fde1" was actually based on cb19cb1d — worth re-resolving origin/main before trusting an early `git log` read; (2) `git read-tree --reset -u` while resolving the merge silently wiped the loop runner's 11 uncommitted JOURNAL.md lines — restored from a diff captured earlier, but read-tree --reset is not safe next to uncommitted sibling files; (3) `git branch -f` onto the linear work commit silently dropped the merge lineage from the PR head — rebuilt with commit-tree parented on the merge so both b4f132e2 and origin/main stay ancestors. Late addition after review: main's FakePredictionAdapter always answers with a FULL fill, so main had NO test of an accepted-but-unfilled or partial ack — the very distinction the issue names. Added filled_override_/status_override_ and two cases (zero-fill -> "resting" remaining 10, no position; 4-of-10 -> "partially_filled" 4/6, no position), neuter-verified by forcing normalized_kalshi_order_state to always return "filled" (both red, nothing else). Lesson: "main already implements it" is not "main already tests it" — check the fixture, not just the code path.
- 2026-07-24 02:50:13 issue #125 PR #44: needs-human-review — left for operator
- 2026-07-24 02:50:14 loop completed 1 iterations
- 2026-07-24 10:05:04 iteration 1: building issue #126 (log: finn/logs/20260724-100504-issue-126.log)
- 2026-07-24 issue #126 PR #132: Kalshi paper bot rung 1 — pure KalshiBotDecision (openterminal_core) + `kalshi bot once|run` in its own SKIP_UNITY TU. Every decision journaled to kalshi-bot-decisions.jsonl, passes included; refusal rows carry NO numbers. Full suite 128/128, tst_kalshi_bot_decision confirmed RUN as Test #79; 12 cases first seen red against a stub decide(), then seven individual neuters each turning exactly its own test red. Surprises: (1) the calibrator's adds_value_over_market flipped true→false mid-iteration on the LIVE file, which accidentally proved the "re-check live, never hardcode yesterday's verdict" criterion — bids flipped to SIGNAL_UNTRUSTED with it; (2) calibrator.json's runway feature (sqrt_minutes_left) is as-of generated_at_ms, so with a 120s staleness bound a contract can lose most of its runway before the read — must subtract elapsed time or the bot bids contracts about to close; (3) settled contracts could be re-bid once ALREADY_HELD stopped applying, and the runway gate does NOT cover it when the daemon snapshot the calibrator reads goes stale under a fresh report — closed with a CONTRACT_SETTLED gate; (4) set_source_files_properties is directory-scoped, so the top-level SKIP_UNITY_BUILD_INCLUSION never reached tst_command_dispatch (which compiles the cli families itself) — surfaced as a real usage() ambiguity, fixed by repeating the property in tests/CMakeLists.txt; (5) kalshi-account-settlements.jsonl only covers markets the ACCOUNT traded, so it settles almost nothing the paper bot bids — kalshi-settlements.jsonl (kalshi_settlement_label rows, every watched market) is the ledger with real coverage, so both are read.
- 2026-07-24 10:53:51 issue #126 PR #132: MERGED
- 2026-07-24 10:53:55 iteration 2: building issue #127 (log: finn/logs/20260724-105355-issue-127.log)
- 2026-07-24 issue #127 PR #133: Kalshi bot rung 2 — pure KalshiBotGate (openterminal_core) + `kalshi bot gate [seal '<json>']` in the existing SKIP_UNITY TU. Four preregistered criteria (>=300 settled bids, net P&L after fees > 0, bot Brier strictly better than the market baseline on the SAME contracts via a position_id join back to the decision row, peak-to-trough drawdown limit) sealed SHA-256 / chmod 0444; re-seal refused while the file exists; TAMPERED and NOT_PREREGISTERED write the refusal with NO criteria and NO ledger numbers. Floors are re-validated on every READ, not only at seal time, so a hand-sealed "min_settled_bids: 5" is refused rather than scored. No override: unknown key refused at seal time, injected key breaks the seal. 26 tests, full suite 129/129, tst_kalshi_bot_gate confirmed RUN as Test #80; five individual neuters each turned exactly their own tests red. Surprises: (1) the first `altered_params` test did NOT bind to the seal — the loosened params also tripped the floor re-validation, so neutering seal_valid() left it green; rewrote it to loosen (500 bids, $5 dd) → (300, $50), both perfectly valid criteria, so only the seal can catch it — the neuter pass is what exposed the weak test, not the green suite; (2) QStringLiteral() cannot wrap a constexpr const char* (the class's own kVerdict* constants) — needs QString::fromLatin1, a silent parse-error wall if you reach for the usual macro; (3) drawdown must start its peak at 0.0, not at the first settlement, or a bot that loses on bid #1 shows zero drawdown.
- 2026-07-24 11:33:18 issue #127 PR #133: needs-human-review — left for operator
- 2026-07-24 11:33:19 iteration 3: building issue #128 (log: finn/logs/20260724-113319-issue-128.log)
- 2026-07-24 issue #128 PR #134: Kalshi bot rung 3 GUI parity — new BOT tab on the Kalshi screen fed by a pure KalshiBotPanelPresentation.h (ArenaContextPresentation pattern), 24 tests, ctest Test #117, full suite 129/129. Key call, made after challenging my own first design: the panel computes NO trading numbers — settled count, net P&L after fees and Brier-vs-market are read out of the gate's published verdict, not re-derived from the ledger, because the gate dedups settlements by position_id and joins calibrated_p back to the opening decision; a GUI reimplementation would eventually disagree with the verdict shown beside it. No gate file therefore means no scoreboard (absent, never zero). Four neuters each turned exactly their own tests red (never-tripping stale threshold; criteria rendered without numbers; bid row stripped of side/price/size; path helper bypassing the evidence-path module). Surprises: (1) kalshi-bot-gate.json has no writer on main — rung 2 (PR #133) is still open — so the screenshot honestly shows GATE NOT EVALUATED and the PASS/FAIL rendering rests on unit tests against the exact evaluate() schema; nothing includes the unmerged module; (2) this machine had an ARMED Kalshi automation session, so the armed-caps branch was exercised against real evidence, and an unreadable status had to be given its own SESSION UNKNOWN / FAIL CLOSED state so a poll that has not answered yet cannot read as DISARMED; (3) finn/evidence/ .md files are gitignored (`!finn/*.md` covers only the top level) — the evidence note ships as .txt; (4) GUI capture needed a synthesized CGEventCreateScrollWheelEvent via ctypes (System Events has no scroll, and pyobjc/Quartz is not installed) after closing the Dashboard frame to widen the pane — Page Down does not reach a QScrollArea from a clicked QLabel.
- 2026-07-24 12:25:21 issue #128 PR #134: review left no verdict (session died?) — retrying review once
- 2026-07-24 12:34:41 issue #128 PR #134: MERGED
- 2026-07-24 12:34:44 iteration 4: building issue #129 (log: finn/logs/20260724-123444-issue-129.log)
- 2026-07-24 issue #129 PR: Kalshi bot rung 4 — observable loop. New shared header KalshiBotRuntime.h holds ONE loop-status classifier (state, colour role, headline, 2-interval staleness constant) plus the kill-switch file format; the GUI chip and `kalshi bot status` are both thin renderers over it, so "the CLI mirrors the chip" is a property, not a claim. Kill switch = the stop file's EXISTENCE (fails closed: junk, empty, even `"stopped": false` still stops), checked at the top of KalshiBotDecision::decide() — the only path from calibrator to bid — so "every tick before any bid" is structural. `kalshi bot stop|resume|status` + one GUI button that writes the same bytes through the same helper (in-process, never shelling out — PR #109's lesson). launchd plist KeepAlive is Crashed-only, deliberately. 131/131 ctest; 4 suites confirmed RUN (#79 decision, #80 runtime, #113 e2e, #119 panel); two independent neuters each turned exactly their own tests red (decide()'s stop check → 2 unit tests + the e2e's BOT_STOPPED assertion; the run loop's exit → the e2e's one-tick assertion, 29s instead of 0s). Real-GUI round trip captured: button wrote source="gui", chip went red with a headline byte-identical to `kalshi bot status`, RESUME cleared it and both went green. Surprises: (1) the honesty hole the issue's literal green/amber/grey list would have left — a killed bot's LAST ledger row is its own refusal, so it reads FRESH; without a 4th STOPPED state taking precedence the chip sits green on a bot that is not bidding, which is why red STOPPED was added beyond the issue text; (2) `kalshi bot status --json` silently printed human text because --json is a GLOBAL flag consumed before the subcommand — a trailing --json was landing in argv and being ignored, so status now refuses unknown options instead of dropping them; (3) `grep -c` exits 1 on zero matches, so the shell idiom `[ -f f ] && grep -c p f || echo 0` prints TWO numbers and every arithmetic test after it dies with "integer expression expected".
- 2026-07-24 13:14:21 issue #129 PR #133: needs-human-review — left for operator
- 2026-07-24 13:14:22 iteration 5: building issue #130 (log: finn/logs/20260724-131422-issue-130.log)
- 2026-07-24 issue #130 BLOCKED (no PR): Kalshi bot rung 5 (micro-live behind the human arm) hard-depends on two unmerged `needs-human-review` PRs — #133 (rung 2 gate: `KalshiBotGate.{h,cpp}`, the writer of `kalshi-bot-gate.json`) and #135 (rung 4 kill switch: `KalshiBotRuntime.h` + the stop-check at the top of `decide()`). Both verified absent from a freshly fetched `origin/main`; `KalshiBotDecision.h` there still documents "this rung has no live mode at all". Three of #130's five criteria name those artifacts directly. No honest route around it: reimplementing the gate reader is scope creep AND recreates the exact divergence rung 3 avoided (the BOT panel reads the gate's published verdict rather than re-deriving it); stacking on the PR branches violates branch-fresh and binds rung 5 to contracts that may still move under review; and a partial build is impossible because #133 and #135 both modify the same three files rung 5 must touch (`KalshiBotCommands.cpp`, `KalshiBotDecision.*`, `KalshiBotPanelPresentation.h`), so any rung 5 branch collides with both at merge. Posted the unblock path and put the one genuinely operator-owned choice — wait for the merges vs. authorize stacking — back to the operator rather than picking it. Surprise: the ladder's own ordering was the trap. Rungs 2 and 4 were BUILT before rung 5 but sit behind personal-merge review by design (carve-out condition 3 makes the gate operator-merged by definition), so a loop that picks the oldest `agent-ready` issue will keep reaching a rung whose prerequisites are complete-but-unmergeable — completed work in a `needs-human-review` queue is not the same as landed work, and only `origin/main` counts.
- 2026-07-24 13:18:36 issue #130: no PR after build (blocked or failed) — see log
- 2026-07-24 13:18:38 iteration 6: building issue #131 (log: finn/logs/20260724-131838-issue-131.log)
- 2026-07-24 issue #131 PR #136: Kalshi bot rung 6 — order lifecycle honesty. New pure `KalshiBotOrders` (replay / reconcile / requotable, own file so it collides with none of the in-flight rung PRs): a bid is now an ORDER with `order_state=resting` and a TTL, not a position. Exposure is PR #44's rule verbatim (filled at fill price + remainder at LIMIT price, over everything not canceled/settled), so a resting quote is money the bot counts. Cancels go through an INJECTED canceller and only a confirmed one writes CANCELED_*; an unconfirmed one journals UNCONFIRMED_CANCEL, leaves the order resting, keeps its exposure, and is retried next tick. 21 new tests + 4 panel tests, full suite 130/130, tst_kalshi_bot_orders confirmed RUN as Test #80; six independent neuters each turned exactly their own tests red. Six real CLI transcripts in finn/evidence/issue-131-order-lifecycle.txt, all against a scratch OPENTERMINAL_KALSHI_EVIDENCE_DIR (the operator's live ledger untouched). Surprises: (1) the design's one real fork — calibrator.json carries a MID and no book at all, so a paper fill cannot be measured; the rule is "a resting limit fills only once an observed mid is at or through it, at the limit, never better", stated on every fill row (`fill_model`/`fill_rule`) the way rung 1 stated its own, and rung 1's rows (no `order_state`) replay as `rung1_assumed` positions so a mixed ledger is readable rather than silently averaged — this DOES change the denominator rung 2's sealed gate scores, which is why it is said out loud rather than shipped quietly; (2) ordering is load-bearing — reconcile must run BEFORE decide and the book must be re-replayed in between, or ALREADY_HELD/QUOTE_RESTING blocks every requote the TTL pass just freed; (3) a nested struct's default member initializers are not parsed yet at the point a defaulted argument of the same class is declared, so `decide(..., const Exposure& = Exposure{})` will not compile — it needs an overload; (4) the first TTL e2e accidentally FILLED instead of expiring because the synthetic mids sat exactly on a cent (limit == mid → marketable at once); real mids are continuous (0.0035, 0.8342), so the floored limit rests strictly below the market and fills stay genuinely conditional — synthetic fixtures can flatter a fill model into looking trivial; (5) review caught the one thing eight green neuters could not — the fill check ran BEFORE the settled check, so a resolved market whose stale snapshot still quoted a marketable mid was journaled FILLED_AT_LIMIT and settled into fabricated P&L (rung 1's CONTRACT_SETTLED trap, re-opened by the lifecycle), and CANCELED_MARKET_SETTLED did not feed the settled set, so the resolved contract — no position, no resting order, and never having filled, no settlement row either — was quoted again next tick. Both seen failing first, both closed, both neuter-verified. The test that should have caught them existed and was GREEN: it used mid 0.86 against an 0.83 limit, which makes the fill branch unreachable, so the assertion never reached the code it was named for. A test written with values that cannot enter the branch is not coverage.
- 2026-07-24 14:25:38 issue #131 PR #135: needs-human-review — left for operator
- 2026-07-24 14:25:39 no unhandled agent-ready issues; loop idle-exits at iteration 7
- 2026-07-24 issue #129 PR #135: forward-ported the rung-4 observable-loop PR onto origin/main after rung 2 (PR #133) landed. Merge 35eccdfa (parents 54bf41e3 + 5e4df456); merge-forward, not rebase, so both tips stay ancestors (`git merge-base --is-ancestor` verified for each before push). Two conflicts, both additive-union — main added `kalshi bot gate [seal]`, the branch added `stop|resume|status` — so nothing was chosen between them: both include lines, all six subcommands in kSubcommands/usage()/the pre-config dispatch block, and all three test targets kept. Three deletions from main's side are supersessions, not weakenings: the 5-arg `decide()` (pre-kill-switch form) → the 6-arg one taking `stop`; the `kalshi-bot-decisions.jsonl` and `60` literals → rung 4's shared constants (same values, one source of truth); main's namespace-level `is_event()` kept and the branch's identical local lambda dropped. Weakening was disproven mechanically, not asserted: `git diff` vs the pre-merge tip is EMPTY for KalshiBotDecision.{h,cpp}, KalshiBotRuntime.h, KalshiBotPanelPresentation.h, KalshiScreen.{h,cpp}, all four test files and the plist, and `git diff` vs origin/main is EMPTY for KalshiBotGate.{h,cpp} + tst_kalshi_bot_gate.cpp — the merge edits exactly two files. Full suite 132/132; #79 decision, #80 runtime, #81 gate, #114 e2e kill switch, #120 panel all confirmed RUN by name and number. Kill-switch neuter re-run post-merge (`if (stop.engaged)` → `if (false)`, .o mtime confirmed changed before trusting it): exactly the expected three assertions red — both unit cases plus the e2e's BOT_STOPPED — and nothing else; reverted, rebuilt, 132/132 again. PR is MERGEABLE, needs-human-review kept, NOT merged. Surprises: (1) the test NAME set, not the count, is what proves a CMakeLists union merge — 130 (main) ∪ 131 (branch) must be 132, and captured both name sets from `git show` before resolving so a dropped target could not hide behind a green suite; (2) `ctest ... | tail -40` in a background task truncates the log to 40 lines, so the "confirmed RAN" evidence was gone and the suite had to be re-run capturing the whole thing — pipe to a file, tail the file; (3) the union merge's one real semantic risk was invisible to git — rung 4 made `status` refuse unknown options while rung 2's `gate seal` takes a POSITIONAL JSON payload, so the two could have collided in a shared arg loop; they did not (the strictness is per-subcommand), verified by driving `gate seal '{...}'` through the real CLI against a scratch evidence dir rather than trusting the green suite.
- 2026-07-24 15:11:15 PR #135: MERGED (operator-authorized game-on sequence, CI green)
- 2026-07-24 15:54:38 issue #130: no PR after build (blocked or failed) — see log
- 2026-07-24 16:30:35 no unhandled agent-ready issues; loop idle-exits at iteration 2
- 2026-07-24 16:30:41 iteration 1: building issue #130 (log: finn/logs/20260724-163041-issue-130.log)
- 2026-07-24 issue #130 PR #140: Kalshi bot rung 5 — micro-live behind the human arm. New pure `KalshiBotLive` (openterminal_core) admits a live tick only when EVERY carve-out condition holds at once — human-armed session, bounded in time, published gate verdict PASS and < 1h old, kill switch clear — else rc 4 with a named refusal journaled mode=live carrying NO trading numbers. A permitted bid becomes an intent for the EXISTING prepare_order/submit_order pair with the armed session's own caps (clamped to the charter ceilings); submit_order re-checks all of them and stays sole authority. FAK, because this rung has no live cancel path and nothing may rest unattended. 34 unit cases + 1 e2e + 1 submit-path case in tst_pm_paper; full suite 135/135; fourteen independent neuters each turning exactly its own test(s) red. Real-GUI capture on a scratch profile/evidence dir (operator's app, ledger and live session untouched): [LIVE] badge red, ARMED caps line, LIVE rows with `submit filled` / `submit rejected`. Surprises: (1) live rows landing in the paper ledger had THREE consumers, not one — replay's conditional-mid fill model, settle_paper, and KalshiBotGate::evaluate, which the CLI feeds mode-blind; that last one would have counted live outcomes as the PAPER evidence carve-out condition 3 requires, i.e. the bot grading itself on the trades its own promotion authorised. Both filters went INSIDE replay/evaluate, not at the call sites, because a caller that forgot one would silently invalidate the seal. (2) `session_active` is not `bounded` — a 24/7 arm writes an empty ends_at and the terminal's own activity test (`!ends.isValid() || ends > now`) reads that as ACTIVE and ARMED; boundedness needs its own check or an unbounded arm gets ridden. (3) submit_order's per-contract duplicate guard does NOT cover a filled LIVE order: it counts drafts in ('submitting','submission_unknown','submitted') but a live submit writes the VENUE's state onto the draft ('filled'/'resting'/…), and only the PAPER branch ever writes 'submitted' — so relying on it would have had the bot re-buy the contract it just bought every tick until a cap stopped it. Proved at the real submit path, then closed bot-side with `live_working()`; filed #141 for the shared hand-approved path, which still has it. (4) `--paper --mode live` ran LIVE — `--paper` was taken and discarded, so the launchd job's whole safety story was a flag a later --mode silently beat; silent ESCALATION is the same defect class as silent degradation, now rc 2. (5) the neuter pass again caught a test that could not fail: `a_paper_bid_does_not_block_a_live_tick` used a paper reason code the accepted-only filter already excluded, so removing the mode check left it green — rewritten with a live reason code and mode=paper so only the mode check can catch it.
- 2026-07-24 17:51:02 issue #130 PR #140: needs-human-review — left for operator
- 2026-07-24 17:51:03 loop completed 1 iterations
- 2026-07-24 17:51:54 iteration 1: building issue #137 (log: finn/logs/20260724-175154-issue-137.log)
- 2026-07-24 18:15:39 issue #137: no PR after build (blocked or failed) — see log
- 2026-07-24 18:15:40 iteration 2: building issue #138 (log: finn/logs/20260724-181540-issue-138.log)
- 2026-07-24 issue #138 PR #142: the ADVISOR & CANARY tab now says the v5 duel concluded instead of rendering 32h-old machinery as a live system. Two pure functions BESIDE an untouched `present_advisor_canary` (the issue's "or a wrapper") — `advisor_duel_retirement()` (retired when every advisor write timestamp is silent past 6h) and `present_advisor_duel_archive()` (the frozen record out of advisor_competition_report.json) — so "a restarted loop renders unchanged" is a property of the diff, not a claim a test has to defend. 9 new cases (suite 133/133, Test #119 confirmed RUN); four neuters each turned exactly their own tests red, in BOTH directions (`retired=false` → the 3 retired cases; `retired=true` → the 3 not-retired cases). Real-GUI round trip on a scratch profile seeded with this machine's actual advisor files: banner + frozen record with the armed badge still above it, then one file made fresh → pre-change rendering back within one 5s tick. Surprises: (1) the advisor caught the load-bearing one before I wrote a line — `latest_legacy_live_status_` is NOT an advisor file but the live `kalshi auto live status` poll, so my first plan ("hide the badges when retired") would have covered an ARMED indicator with "DUEL CONCLUDED"; retirement now reads the six files only, the legacy badge is never hidden, and the scratch-profile screenshot happens to show ARMED sitting above the banner — the exact case a unit test had to encode because a no-account profile can't be relied on to produce it; (2) the "final duel record" is a trap for fabrication — result_state is INSUFFICIENT_PAIRED_DATA (53/200 jointly resolved) and the epoch_pair is claude-**v5** vs codex-**v4**, so both "the v5 duel" and any winner derived from paired_brier_delta (-0.0026) would have been half-invented; the banner prints the epochs verbatim and says NO WINNER DECLARED; (3) advisor_competition_report.json is 766KB against a 5s refresh tick — read at most once per session, gated on retirement, or the tab reparses it 720 times an hour; (4) `git checkout -b` from origin/main aborts while the loop runner's uncommitted JOURNAL lines sit on main — stash first, and keep a `git diff` copy in /tmp in case the pop is ever lost.
- 2026-07-24 18:59:58 issue #138 PR #142: MERGED
- 2026-07-24 19:00:00 iteration 3: building issue #139 (log: finn/logs/20260724-190000-issue-139.log)
- 2026-07-24 issue #139 PR #143: the AUTO COCKPIT now says whether its own inputs are fresh. New pure `AutoCockpitPresentation.h` (22 cases, full suite 134/134, Test #122 confirmed RUN; ctest NAME set = previous 110 ∪ exactly one) classifies the cockpit's contract list and ladder book feed live/stale/absent with green/amber/grey, and the header renders on the 1s CLOCK tick rather than inside the 5s ladder engine — which returns early with no selection, so the old header genuinely froze at its last-good text with nothing ageing against it. A not-live cockpit replaces the ladder rows with the stated reason instead of keeping last-good prices; `record_ladder_evidence` skips `render_ladder_surface` in that case so the two owners never fight over the table. Book ages come from `KalshiSurfacePoint::quote_observed_at_ms` on the surface the ladder is drawn from, never a second lookup into `kalshi_books_`. Seen failing first: a stub that always answers live/green turned 12 of 22 red; then three independent neuters each turned exactly its own tests red (never-stale timestamp → 8, always-trustworthy ladder → 9, never-absent books → 1). Deliberately did NOT rebuild the periodic market refetch: that is #137's, whose branch already exists unmerged and touches the same two files — cited on the issue instead, and this header is what makes it visible. Surprises: (1) `quote_observed_at_ms == 0` is unambiguous — BOTH book producers always stamp a time (`KalshiRestClient.cpp:772`, and `KalshiWsClient.cpp:491-493` falls back to now) — so a zero means "no book for this leg", which is what let the silent `quote_for` fallback be counted honestly rather than guessed at; the live capture read 0/188 legs quoted, cross-checked in the ledger (54 of the last 108 surface rows have zero quoted legs, interleaved 1:1 with the operator's 188/188 rows). (2) The label whose text literally says "AUTO COCKPIT" was laid out on the MAKER ORDER tab and the cockpit tab had no header at all — moved, not duplicated, or the screen would have carried two competing headers one of which still freezes. (3) The first real-window capture rendered the new chip as a BLANK BOX: the cockpit tab had no scroll area unlike its three siblings, so the fixed-height pane compressed it to zero height — a header that renders empty is the same "reads as broken" failure the issue is about, and no unit test could see it; the cross-link then vanished for a second layout reason (beside a word-wrapped chip it loses its width). Only looking at the real window caught either. (4) `HOME=` does NOT isolate the Kalshi evidence directory — `evidence_path` goes through `QStandardPaths::AppLocalDataLocation`, which on macOS comes from the process's real home, so the scratch-profile session appended ladder rows to the operator's shared ledger; `--profile` isolates the profile, not the ledger. (5) The machine was locked all session (`loginwindow` at window layer 2004), so full-screen capture returned black and synthesized `CGEventPost` clicks went to the lock window — `screencapture -l<windowid>` still reads a window's own buffer, and the tab had to be pre-selected by a one-line capture-only build that is not in the branch.
- 2026-07-24 issue #139 PR #143 (review round 2, `545570e6`): the reviewer was right and the defect was worse than my first fix admitted — `record_ladder_evidence` wrote the plan summary `cost $… · expected $… · worst $…` UNCONDITIONALLY and only then consulted `ladder_trustworthy`, which guarded `render_ladder_surface` alone. Not a timer race: the notice written by `refresh_auto_cockpit_header()` at :3171 was overwritten 156 lines later in the SAME call, before Qt repainted, so on every not-live pass the header call I had added inside the engine pass was dead code with respect to `ladder_status_`. Fixed by gating all three writes (setText, the green-on-opportunity setStyleSheet, render_ladder_surface) behind one `if (!cockpit_view.ladder_trustworthy) return;`. Two traps the minimal fix would otherwise have kept: (1) the view must be hoisted AFTER :3157-3170, since `auto_cockpit_inputs()` reads `cockpit_ladder_` which only that block repopulates — hoisting to the top gates on the PREVIOUS pass's leg freshness and nothing in the suite goes red; (2) `refresh_auto_cockpit_header` read its own `currentMSecsSinceEpoch()`, a LATER clock than the gate's `now` from :3095 with REST/DB work in between, so a staleness boundary crossed mid-pass reproduces the identical overwrite with both writes nominally gated — it now takes `now` as a parameter, one pass one reading. Suite 134/134, Test #122 RUN, 24 cases. HONEST GAP STATED, not implied: no test fails without this fix and none was added that would — the defect is GUI wiring and there is no KalshiScreen harness; a pure `auto_cockpit_plan_summary(view, plan_line)` helper routing both writers would still pass if someone re-added a bare `ladder_status_->setText(...)` to the engine, so it buys structure not coverage, and the advisor called it correctly as scope creep on a header the reviewer had already verified line by line. Surprises: (1) three figures in my own PR body were false and the reviewer caught all three — "previous 110" ctest names (actually 133), the `timestamp_stale` neuter at 8 red (actually 10), `ladder_trustworthy` at 9 (actually 10); the first two understated because they predated the last two test cases, i.e. a neuter count goes STALE the moment you add a case, and quoting it later is fabrication by staleness. Re-ran all four neuters one at a time, restoring to 24/24 between each, rather than copying the reviewer's numbers. (2) The reviewer's `#include <algorithm>` nit was itself wrong — it has been at KalshiScreen.cpp:79 since 6ac75db2; checked `git show origin/main:` before adding a duplicate, and said so rather than silently skipping it. Never argue more than twice, but do verify before complying.
- 2026-07-24 20:51:23 issue #139 PR #143: merge failed — see log
- 2026-07-24 20:54:54 iteration 4: building issue #141 (log: finn/logs/20260724-205454-issue-141.log)
- 2026-07-24 21:54:16 issue #141: no PR after build (blocked or failed) — see log
- 2026-07-24 21:55:38 loop completed 4 iterations
- 2026-07-24 22:03:25 PR #140: review's screenshot request addressed (evidence-only revision) — awaiting operator merge
- 2026-07-24 22:12:34 issue #139 PR #143: MERGED (loop-approved; retried through network outage)
- 2026-07-24 issue #137 PR #144: resumed an abandoned branch — the 15-minute contract auto-roll. Treated both inherited commits as unverified: merge-forward onto origin/main fe2ac0cd (merge 3e47dc2f, clean), then proved non-weakening mechanically rather than asserting it — the added/removed LINE SETS of HEAD vs the branch tip are identical to main's own change, and HEAD vs origin/main identical to the branch's own change, for KalshiScreen.cpp, KalshiScreen.h and tests/CMakeLists.txt; ctest NAME set = main's 134 ∪ {tst_kalshi_market_roll} = 135, as sets not counts. Suite 135/135, Test #124 RUN, 24 cases (the inherited commit message said 25 — measured, not copied). Six neuters one at a time, restored to 24/24 between each: in-flight guard 2 red, is_expired-always-false 10, preservation branch 3, is_selected_contract 3, stall escape 1, close grace 1. Surprises: (1) the fix's real hinge is invisible to every unit test — the roll only reaches the DOM and charts if setCurrentRow re-enters select_market, and a QListWidget that did NOT reset its current row on clear() would make a roll onto the same row index a silent no-op, i.e. the reported bug surviving a green suite; proved with a scratch Qt program replaying populate_markets' exact clear/addItem/setCurrentRow sequence (clear emits -1 → early return, then every setCurrentRow re-enters). (2) git merged 275 lines of #142/#143 into the same functions cleanly, but ONE placement was semantic: #143's `cockpit_ladder_ = AutoCockpitInputs{}` landed after this branch's teardown-skip, which is correct (reset book ages on a real contract change, keep them when a background refresh re-lists the same one) — a textually identical merge on the other side of that early return would have zeroed freshness every 30s. (3) OPENTERMINAL_KALSHI_EVIDENCE_DIR is the env var that actually isolates the Kalshi ledger; #139 learned the hard way that HOME does not, and it is right there in ServeCommand.h:204. (4) The capture failed and the failure is the finding: machine at the login screen again, the Prediction Markets dock panel would not stay materialised (state=spawning, has_live_widget=false), a QT_QPA_PLATFORM=offscreen run never reached 'Application ready', and the network was degraded (11.5s for one Kalshi REST call). One real 15-min-cadence fetch was captured via a capture-only `cadence_ = fifteen_min` build (not in the branch); no rollover was hand-verified and the PR says so. (5) My own restarts rotated away the earlier session's log, so its 30s-cadence figures became unverifiable — described in the evidence note, deliberately NOT re-quoted as evidence.
- 2026-07-25 issue #137 PR #144: REVIEW — loop-changes-requested. Criteria 2–4 (auto-roll, selection preservation, no-storm) verified independently: rebuilt tst_kalshi_market_roll from the PR head standalone against Qt (26 passed / 24 cases), then ran five neuters one at a time restoring to 26/26 between each — preservation branch 3 red, in-flight guard 2, close grace 1, stall escape 1, is_expired-always-false 10, matching the PR's table exactly rather than being taken on trust. Also checked by hand that Kalshi.fetch_category.markets/.events are real request_error contexts (KalshiRestClient.cpp:500,632,642), so the in-flight release is not dead code, and that has_selection_ is only set true after the new short-circuit. Charter clean: no frozen file, no order/arming path (the price_->hasFocus() guard reduces stray ticket writes), non-goals held, no new CLI TU. Blocked on criterion 5's second half — "manual evidence across a real 15-min rollover" — which the PR itself says was never captured. Asked for two minimal things: a log line carrying decide_market_refresh's reason, and one live observation of a second list fetch ~30s after the first. Surprises: (1) KalshiScreen.cpp:1956 is the screen's FIRST use of isVisible(), and there is no hideEvent, so isVisible() is the sole suppressor of a tick once the timer starts — no prior working path on that predicate to argue by analogy from; the fix is not inert, though, because market_list_timer_->start() is only called from showEvent, so it starts visible by construction, which is what rules out last session's "the screen went not-visible" reading in favour of "the panel never materialised and nothing was exercised". (2) Nothing in the running process reports which refresh branch was taken, which is exactly why that ambiguity could not be resolved from the logs — hence change request 1. (3) zsh does not word-split unquoted variables, so a flags-in-a-variable build helper silently passed one giant argument and every neuter "ran" as COMPILE FAILED; caught only because the harness checked for the binary before trusting a result. Worth keeping: never trust a neuter that did not print a Totals line.
- 2026-07-25 01:42:43 issue #137 PR #144: not auto-merged (labels: needs-scope-approval loop-changes-requested ) — see log
- 2026-07-25 11:00:02 filed BOT-panel fail-open-[LIVE] issue
- 2026-07-25 11:48:39 filed: https://github.com/Trucker2827/Open-Terminal/issues/146 (BOT COCKPIT (Kalshi))
- 2026-07-25 12:52:14 PR #140: evidence-revision attempt finished — see finn/logs/20260724-evening-recovery.log and the PR
- 2026-07-25 issue #137 PR #144: REVIEW round 2 — loop-approved. Both round-1 change requests met and re-derived rather than read: the two tests added by `4227c5ca` (`every_refresh_branch_names_itself_in_the_log`, `the_log_line_names_both_ends_of_a_roll`) had never been independently neutered, since round 1 built at `700ecc73` which predates them — built the target at HEAD `2ccc62b5` (28 passed / 0 failed), then two neuters one at a time in `MarketRollPresentation.h` (`reason=%2`→`why=%2`; ` -> ` dropped from the selection line), each turning exactly its own test red, binary relinked each time, restored to 28/28 between and after, tree clean. Deliberately did NOT re-run round 1's five neuters — `4227c5ca` does not touch the code they exercise, so that is spend without discrimination. Criterion 5's manual clause settled from primary evidence, not the reply: `kalshi-lifecycle.jsonl` holds 18 `kalshi_screen_contract_roll` rows, all `live_eligible:false`, including real quarter-hour boundaries (`KXBTC15M-…1745-45` close 21:45:00Z → `…1800-00` at 21:46:30Z). Raw cadence log counted directly: 74 `periodic`, 131 `in-flight`, 14 `waiting`, 64 `expired`, ZERO `hidden`, two `ROLLED`, one `vanished` — the honesty rule holding under a real payload (absent-but-not-expired → no record) is worth more than the roll itself. Surprises: (1) the charter says "CI green on all three platforms", but this repo's PR CI is Linux-only BY DESIGN — `regression.yml` triggers on `pull_request`, `build-cpp.yml` is `workflow_dispatch` only — so "two checks green" IS green CI here and holding a PR for absent macOS/Windows runs would be inventing a gate; check the workflow triggers before reading a missing platform as a failure. (2) `refresh()` gained a defaulted `bool background` and is connected to `QPushButton::clicked(bool)` at KalshiScreen.cpp:1556 — Qt binds the signal's bool to that parameter, and it is correct today only because `refresh_button` is not checkable; one `setCheckable(true)` away from an operator click silently becoming a background refresh. A defaulted bool on a slot is a trap worth grepping for. (3) `KalshiScreen::evidence_path()` uses `QStandardPaths::AppLocalDataLocation` while only the CLI reads `OPENTERMINAL_KALSHI_EVIDENCE_DIR`, so the previous iteration's note (3) is WRONG in the direction that matters: the env var does not isolate the SCREEN's writes, which is why those 18 roll rows are in the operator's shared ledger. Neither `--profile` nor the env var isolates GUI evidence writes; worth its own issue. (4) My label POST at 22:42:18 removed `loop-changes-requested` as a side effect (repo automation), so the explicit DELETE 404'd — checked the timeline rather than shrugging, and it also confirmed no commits after `2ccc62b5`, i.e. the review was against the live head.
- 2026-07-25 18:44:27 issue #137 PR #144: MERGED (revision round: log line + live evidence)
- 2026-07-25 18:44:27 issue #137 PR #144: MERGED (auto-roll live)
- 2026-07-25 18:44:29 iteration 1: building issue #141 (log: finn/logs/20260725-184429-issue-141.log)
- 2026-07-25 issue #141 PR: submit_prediction_order's per-contract duplicate guard now sees a FILLED live draft. The guard counted ('submitting','submission_unknown','submitted') — but only the PAPER branch ever writes 'submitted'; a LIVE submit writes the VENUE's state ('filled','partially_filled','resting','accepted'), so a filled live order left a draft the IN-list missed and a second live order on the same contract went through. IN-list widened to every status meaning "the venue has, or may have, an order", including `reconciliation_required` (the venue took it; only the local ledger write failed) — a TIGHTENING of the #39/#44 gate, never a weakening. With the shared guard covering it, PR #140's bot-side suppression `KalshiBotLive::live_working()` and its four tests are deleted, and the live tick now passes `{}` as the book. Full suite 137/137, tst_pm_paper confirmed RUN as Test #39 and tst_kalshi_bot_live as #83; two neuters, each turning exactly its own test red — reverting the IN-list to the old three reddened only the filled-duplicate case, and adding 'rejected','cancelled' to it reddened only the refused/killed case. Surprises: (1) whitelist vs blacklist is load-bearing and not a style choice — the obvious `NOT IN ('rejected','cancelled','submit_failed')` would have caught 'prepared' too, and the bot writes a 'prepared' draft on EVERY tick before submitting, so one gate rejection would have wedged that contract until the TTL sweep expired the orphan; the honest fix refuses more, not more things; (2) `submission_unknown` in the IN-list is dead weight — the venue-freeze check at OrderFlowTools.cpp:892 already rejects EVERYTHING for a venue with an unknown-outcome draft, with a different reason, and it runs first, so a test built on that predecessor would fail for the wrong reason; kept in the list anyway so the guard is true independent of check order; (3) deleting live_working() is a real behaviour trade, disclosed rather than hidden: the bot now re-bids a held contract every tick and is refused at submit_order instead of passing ALREADY_HELD before prepare, so each tick leaves an orphan `prepared` draft until the sweep expires it — noisier, but the refusal now comes from the authority that owns the drafts rather than from the bot's own ledger; (4) the pilot bullet I first wrote was too generous and the advisor caught it — `kalshi auto live prepare-next`'s candidate SQL excludes a market with a live `trade_audit` FILLED row, and separately drafts in ('prepared','submitting','submission_unknown','submitted'), so a RESTING or PARTIALLY_FILLED predecessor is in neither list and audits as neither; a pilot market with an order still working at the venue could previously be prepared AND submitted again, and this PR newly refuses it. That is the charter's own "resting counts as risk", so it is a deliberate tightening on the hand-approved path, now pinned by `a_live_order_still_resting_at_the_venue_blocks_the_contract` — and `resting`, not `filled`, is the widest state in the widened list, because a micro-live evidence draft is submitted at the immutable human limit PRECISELY so it can rest.
- 2026-07-25 19:23:54 issue #141 PR #147: needs-human-review — left for operator
- 2026-07-25 19:23:55 iteration 2: building issue #145 (log: finn/logs/20260725-192355-issue-145.log)
- 2026-07-25 issue #145 PR: the BOT badge now fails CLOSED on a ledger row this build cannot read. The badge is read off the newest TICK whose `mode` is explicitly `live` or `paper`; if any row NEWER than that one is unreadable (unknown `event`, or a decision row whose `mode` is absent/unrecognised) the badge is `[UNKNOWN]` plus the launchctl-kickstart hint, never `[LIVE]`. Freshness moved off `ts_ms`-only onto `kalshi_bot_row_ts_ms()` (ISO `ts` fallback), so a dated row proves a tick whatever its vintage. "Newest" compares timestamps, not file position, because two appenders (launchd loop + hand-run `bot once`) interleave. Recognised paper settlements do NOT invalidate a live badge — a settlement is paper by construction and is not a tick, so treating it as one would mask real money at the exchange (advisor caught this before it shipped). Suite 137/137; 6 of 7 new assertions seen red under a behaviour-neuter (badge latch, unknown-mode, ISO freshness, hint, ts-order, plus the runtime unit); real-GUI witness on a scratch profile + scratch evidence dir: amber chip reading `[UNKNOWN] BOT STALE · … · bot binary older than GUI — launchctl kickstart -k gui/$UID/org.openterminal.kalshi-bot`. Surprises: (1) the issue's stated cause (b) is WRONG and the PR says so — the production ledger is 32,561 rows, 100% `mode=paper`, every row carrying `event`, `ts` and `ts_ms`, so no old-schema row could have produced that badge; the only gap in the file is 16:03Z 07-24 → 04:34Z 07-25, i.e. the "15h ago" the operator saw was an HONEST reading of a genuinely dead loop (cause (a)), and the [LIVE] half of the screenshot is unexplained by any code on main. Fixed the fail-open the criteria describe anyway, since it is real: a live tick followed by rows of an unknown vintage kept the badge on LIVE. (2) The first neuter attempt was a false pass — reverting the two headers made the tests fail to COMPILE, so ctest ran the stale binaries from the previous build and printed 42 passed; the real neuter keeps the new API and reverts only the behaviour. Never trust a neuter whose build printed errors. (3) `first process whose unix id is <pid>` in System Events matched the WRONG OpenTerminal instance and moved the operator's window; the reliable discriminator is the window title, which carries the profile name (`Open Terminal [finn145] — …`). Restored it before continuing.
- 2026-07-26 issue #146 PR: the BOT COCKPIT — a decision-rain scene over the bot's own evidence, reachable from the BOT tab and suggested while the loop runs. The load-bearing decision was taking `KalshiBotPanelView` as an INPUT rather than re-reading the ledger: state, mode and the kill switch therefore come from the classifier `kalshi bot status` renders from, so "an all-paper ledger can never render the LIVE mood" is a property of the wiring, not a test. Same reflex on every number: the KPI strip is `kalshi-bot-gate.json`'s own `ledger` block verbatim (settled/W-L/net/drawdown, cap from `params`), the gate node prints the BOT panel's own `criterion_line`, and the freeze bound is `KalshiBotDecision::Config::max_report_age_ms` — frozen rain means "the bot would refuse this report too", not a threshold the scene invented. 30 cases, suite 138/138, ctest NAME set = main's 137 ∪ exactly {tst_kalshi_bot_cockpit}; ten neuters one at a time, restored to 30/30 between each, each turning exactly its own tests red (freeze 3, mood-ignores-live 2, motion-always-true 4, absent-gate-scores-zero 2, ignitions-per-ticker 1, missing-mid-reads-zero 1, silent-column-cap 1, always-suggested 1, span-as-age 1, net-not-coloured 1). Real-GUI capture on a scratch profile whose evidence dir was a 3s mirror of the operator's real files, twice: PAPER moving, then the mirror stopped and the same session frozen — every column tagged FROZEN, scene greyed, census stating the age. Surprises: (1) I drove the WRONG APP for fifteen minutes — a leftover instance from 18:46 was still running out of the same build dir, `System Events … process "OpenTerminal"` picked it, and the cockpit button "missing" from its BOT tab was simply a binary two hours old; the title bar said `[finn146]` on mine and not on it, and I read past that twice. Address windows by `unix id`, and check `ps -o lstart` against the binary's mtime BEFORE concluding a feature did not render. (2) `strings` finds nothing in a Qt binary — QStringLiteral stores UTF-16, so "is my change in this build?" needs `data.count(s.encode('utf-16-le'))`; the first check said 0 and was wrong. (3) `QStringLiteral(kSomeConstexprAuto)` does not compile — the macro needs a literal, not a `constexpr auto` — which is why the repo's own path constants are always `QString::fromLatin1(...)`. (4) A QtTest `QSet<QString>(f().begin(), f().end())` over two calls to a QStringList-returning helper is iterators into two different temporaries: SIGSEGV mid-suite, not a failed assertion. (5) `finn/evidence/*.md` is silently gitignored (`!finn/*.md` un-ignores only the top level), so a written-up evidence note vanishes from `git status` — the directory's convention is `.txt`/`.png` for a reason. (6) The advisor caught that my parity evidence was GUI-to-GUI when criterion 3 says "matches the CLI"; running `openterminalcli kalshi bot status` against the same scratch dir on the FROZEN capture is the stronger proof anyway — both readers agreeing on STALE at the same 120s threshold, with the 2-minute offset between the two readings closing arithmetically against the newest ledger row.
