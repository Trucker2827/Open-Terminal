# Finn Loop Journal

One line per iteration; newest last.
- 2026-07-24 issue #103 PR: Equity Research NOTE action closes the research chain into Notes — toolbar NOTE composes a FinancialNote (tickers=symbol, dated title, quote-snapshot body only when a quote loaded — honesty guard, never zero-filled), publishes notes.created (same event/payload as MCP notes tools, so an open NotesScreen live-refreshes), and offers "Open in Notes" → nav.switch_screen + notes.select_note. New pure header EquityResearchNote.h + tst_equity_research_note (6 cases); neuter-checked red (both honesty cases fail without the guard); full suite 127/127. Surprise: a complete prior WIP for this issue sat uncommitted on main atop a 107-line local JOURNAL accumulation — carried the code onto a fresh origin/main branch, stashed to preserve the local journal, kept it out of the PR.
- 2026-07-24 00:58:29 issue #103 PR #123: MERGED
- 2026-07-24 00:58:31 no unhandled agent-ready issues; loop idle-exits at iteration 2
- 2026-07-24 01:17:34 iteration 1: building issue #124 (log: finn/logs/20260724-011734-issue-124.log)
- 2026-07-24 issue #124 PR #39: rebased the disarmed AI live-path PR onto current origin/main (167 commits behind; #40 daemon-caps and #41 venue-scoped-edge/short-side already merged). Merge-forward (one auditable merge commit) over rebase for a needs-human-review live-path PR. Two source conflicts — AiRunCommand.cpp (kept the branch's gated live-mode enablement, layered in main's --market #38 fix) and ServeCommand.h (took main's Kalshi-flow decls, dropped a dup QJsonObject include). Gate-preservation proven by 0-diff vs the pre-merge branch tip: StrategyRunner.cpp/.h and LivePnl.cpp/.h unchanged; OrderFlowTools's only merge delta is #47's Kalshi-ledger honesty work with NO submit_order authority line removed. Full ctest 127/127; e2e_strategy_loop.sh green; two representative gates neuter-verified (phantom-fill → 3 lifecycle/cap tests red; live notional-cap → e2e containment step red). PR un-drafted, needs-human-review kept, NOT merged (operator merges personally). Surprise: the branch lived in the phase2 worktree 1 commit behind origin and the e2e defaults CLI to a stale /tmp/ot-build-ht binary (old "live not supported" behavior) — must pass CLI=<fresh build> or the disarmed-refusal step misfires.
- 2026-07-24 01:54:03 issue #124 PR #39: needs-human-review — left for operator
- 2026-07-24 01:54:04 loop completed 1 iterations
- 2026-07-24 01:54:34 iteration 1: building issue #124 (log: finn/logs/20260724-015434-issue-124.log)
- 2026-07-24 issue #124 PR #39: NO-OP re-verification — issue was already built by the previous iteration and needs no second PR (hard limit: one issue, one PR). Independently confirmed on origin/feat/ai-daemon-loop: 0 commits behind origin/main; CI green on the exact pushed head (Tests & Selftests Linux pass, scope-check pass); PR un-drafted, needs-human-review kept, unmerged. Re-checked the gate-preservation claim myself rather than trusting the PR body: git diff a4518923..head is empty for StrategyRunner.cpp/.h and LivePnl.cpp/.h, the really_filled live gate still keys on broker_status==kLiveFilledStatus (live_submitted otherwise, no paper ai_fill in live), and the AiRunCommand live-containment block still requires floor + bounded session + positive notional/position caps and rejects --max-aggregate-qty. No local rebuild: this iteration adds zero diff, so a suite rerun would verify nothing CI on the same tree has not. Surprise: the issue still carries agent-ready, so only the explicit issue-number input surfaced it (auto-pick skips issues an open PR already references) — labels left alone; stale needs-scope-approval on #39 is cosmetic since scope-check now passes.
- 2026-07-24 02:03:25 issue #124 PR #39: needs-human-review — left for operator
- 2026-07-24 02:03:25 loop completed 1 iterations
- 2026-07-24 02:04:17 iteration 1: building issue #125 (log: finn/logs/20260724-020417-issue-125.log)
- 2026-07-24 issue #125 PR #44: forward-ported the 165-commit-stale Kalshi micro-live pilot accounting branch onto current origin/main (merge aac687cc; parents b4f132e2 + cb19cb1d). Main had independently landed the kalshi_live_orders execution ledger, so two acceptance criteria (accepted/partial/filled distinction via normalized_kalshi_order_state; no fill booked from a bare ack) were ALREADY MET by main — cited, not rebuilt, and the branch's trade_audit-based SQL was resolved in favour of main's ledger. Re-applied only the surviving gaps: kalshi_live_experiment_status now reads the same ledger the submit gate reserves against (it counted decision='filled' while submit reserved resting orders — the strip under-reported live risk), the stake+fee all-in ceiling at submit, an armed-session id required for EVERY micro-live draft (main required it only for autonomous ones), tightening-only session presets (--max-stake/--max-orders-per-hour/--max-all-in/--experiment-cap, above-ceiling refused rc 2 not clamped), and a strip that says "accepted"/"reserved" with the session's own limits. Deliberately DROPPED the branch's session_id scoping of cumulative exposure and the hourly count: it would let a re-arm reset lifetime exposure — a weakening no criterion asked for. Full suite 127/127; four independent neuter runs each turn exactly one expected test red (all-in gate, session-id gate, status accounting SQL, preset propagation); the all-in test carries a within-ceiling control that reaches the adapter so the rejection cannot be attributed to the stake cap. PR un-drafted, needs-human-review kept, NOT merged. Surprises: (1) origin/main moved under me mid-iteration (the loop runner's own fetch absorbed PR #39), so the branch I cut from "1209fde1" was actually based on cb19cb1d — worth re-resolving origin/main before trusting an early `git log` read; (2) `git read-tree --reset -u` while resolving the merge silently wiped the loop runner's 11 uncommitted JOURNAL.md lines — restored from a diff captured earlier, but read-tree --reset is not safe next to uncommitted sibling files; (3) `git branch -f` onto the linear work commit silently dropped the merge lineage from the PR head — rebuilt with commit-tree parented on the merge so both b4f132e2 and origin/main stay ancestors. Late addition after review: main's FakePredictionAdapter always answers with a FULL fill, so main had NO test of an accepted-but-unfilled or partial ack — the very distinction the issue names. Added filled_override_/status_override_ and two cases (zero-fill -> "resting" remaining 10, no position; 4-of-10 -> "partially_filled" 4/6, no position), neuter-verified by forcing normalized_kalshi_order_state to always return "filled" (both red, nothing else). Lesson: "main already implements it" is not "main already tests it" — check the fixture, not just the code path.
- 2026-07-24 02:50:13 issue #125 PR #44: needs-human-review — left for operator
- 2026-07-24 02:50:14 loop completed 1 iterations
- 2026-07-24 10:05:04 iteration 1: building issue #126 (log: finn/logs/20260724-100504-issue-126.log)
- 2026-07-24 issue #126 PR #132: Kalshi paper bot rung 1 — pure KalshiBotDecision (openterminal_core) + `kalshi bot once|run` in its own SKIP_UNITY TU. Every decision journaled to kalshi-bot-decisions.jsonl, passes included; refusal rows carry NO numbers. Full suite 128/128, tst_kalshi_bot_decision confirmed RUN as Test #79; 12 cases first seen red against a stub decide(), then seven individual neuters each turning exactly its own test red. Surprises: (1) the calibrator's adds_value_over_market flipped true→false mid-iteration on the LIVE file, which accidentally proved the "re-check live, never hardcode yesterday's verdict" criterion — bids flipped to SIGNAL_UNTRUSTED with it; (2) calibrator.json's runway feature (sqrt_minutes_left) is as-of generated_at_ms, so with a 120s staleness bound a contract can lose most of its runway before the read — must subtract elapsed time or the bot bids contracts about to close; (3) settled contracts could be re-bid once ALREADY_HELD stopped applying, and the runway gate does NOT cover it when the daemon snapshot the calibrator reads goes stale under a fresh report — closed with a CONTRACT_SETTLED gate; (4) set_source_files_properties is directory-scoped, so the top-level SKIP_UNITY_BUILD_INCLUSION never reached tst_command_dispatch (which compiles the cli families itself) — surfaced as a real usage() ambiguity, fixed by repeating the property in tests/CMakeLists.txt; (5) kalshi-account-settlements.jsonl only covers markets the ACCOUNT traded, so it settles almost nothing the paper bot bids — kalshi-settlements.jsonl (kalshi_settlement_label rows, every watched market) is the ledger with real coverage, so both are read.
- 2026-07-24 10:53:51 issue #126 PR #132: MERGED
- 2026-07-24 10:53:55 iteration 2: building issue #127 (log: finn/logs/20260724-105355-issue-127.log)
- 2026-07-24 issue #127 PR #133: Kalshi bot rung 2 — pure KalshiBotGate (openterminal_core) + `kalshi bot gate [seal '<json>']` in the existing SKIP_UNITY TU. Four preregistered criteria (>=300 settled bids, net P&L after fees > 0, bot Brier strictly better than the market baseline on the SAME contracts via a position_id join back to the decision row, peak-to-trough drawdown limit) sealed SHA-256 / chmod 0444; re-seal refused while the file exists; TAMPERED and NOT_PREREGISTERED write the refusal with NO criteria and NO ledger numbers. Floors are re-validated on every READ, not only at seal time, so a hand-sealed "min_settled_bids: 5" is refused rather than scored. No override: unknown key refused at seal time, injected key breaks the seal. 26 tests, full suite 129/129, tst_kalshi_bot_gate confirmed RUN as Test #80; five individual neuters each turned exactly their own tests red. Surprises: (1) the first `altered_params` test did NOT bind to the seal — the loosened params also tripped the floor re-validation, so neutering seal_valid() left it green; rewrote it to loosen (500 bids, $5 dd) → (300, $50), both perfectly valid criteria, so only the seal can catch it — the neuter pass is what exposed the weak test, not the green suite; (2) QStringLiteral() cannot wrap a constexpr const char* (the class's own kVerdict* constants) — needs QString::fromLatin1, a silent parse-error wall if you reach for the usual macro; (3) drawdown must start its peak at 0.0, not at the first settlement, or a bot that loses on bid #1 shows zero drawdown.
- 2026-07-24 11:33:18 issue #127 PR #133: needs-human-review — left for operator
- 2026-07-24 11:33:19 iteration 3: building issue #128 (log: finn/logs/20260724-113319-issue-128.log)
- 2026-07-24 issue #128 PR #134: Kalshi bot rung 3 GUI parity — new BOT tab on the Kalshi screen fed by a pure KalshiBotPanelPresentation.h (ArenaContextPresentation pattern), 24 tests, ctest Test #117, full suite 129/129. Key call, made after challenging my own first design: the panel computes NO trading numbers — settled count, net P&L after fees and Brier-vs-market are read out of the gate's published verdict, not re-derived from the ledger, because the gate dedups settlements by position_id and joins calibrated_p back to the opening decision; a GUI reimplementation would eventually disagree with the verdict shown beside it. No gate file therefore means no scoreboard (absent, never zero). Four neuters each turned exactly their own tests red (never-tripping stale threshold; criteria rendered without numbers; bid row stripped of side/price/size; path helper bypassing the evidence-path module). Surprises: (1) kalshi-bot-gate.json has no writer on main — rung 2 (PR #133) is still open — so the screenshot honestly shows GATE NOT EVALUATED and the PASS/FAIL rendering rests on unit tests against the exact evaluate() schema; nothing includes the unmerged module; (2) this machine had an ARMED Kalshi automation session, so the armed-caps branch was exercised against real evidence, and an unreadable status had to be given its own SESSION UNKNOWN / FAIL CLOSED state so a poll that has not answered yet cannot read as DISARMED; (3) finn/evidence/ .md files are gitignored (`!finn/*.md` covers only the top level) — the evidence note ships as .txt; (4) GUI capture needed a synthesized CGEventCreateScrollWheelEvent via ctypes (System Events has no scroll, and pyobjc/Quartz is not installed) after closing the Dashboard frame to widen the pane — Page Down does not reach a QScrollArea from a clicked QLabel.
- 2026-07-24 12:25:21 issue #128 PR #134: review left no verdict (session died?) — retrying review once
- 2026-07-24 12:34:41 issue #128 PR #134: MERGED
- 2026-07-24 12:34:44 iteration 4: building issue #129 (log: finn/logs/20260724-123444-issue-129.log)
- 2026-07-24 issue #129 PR: Kalshi bot rung 4 — observable loop. New shared header KalshiBotRuntime.h holds ONE loop-status classifier (state, colour role, headline, 2-interval staleness constant) plus the kill-switch file format; the GUI chip and `kalshi bot status` are both thin renderers over it, so "the CLI mirrors the chip" is a property, not a claim. Kill switch = the stop file's EXISTENCE (fails closed: junk, empty, even `"stopped": false` still stops), checked at the top of KalshiBotDecision::decide() — the only path from calibrator to bid — so "every tick before any bid" is structural. `kalshi bot stop|resume|status` + one GUI button that writes the same bytes through the same helper (in-process, never shelling out — PR #109's lesson). launchd plist KeepAlive is Crashed-only, deliberately. 131/131 ctest; 4 suites confirmed RUN (#79 decision, #80 runtime, #113 e2e, #119 panel); two independent neuters each turned exactly their own tests red (decide()'s stop check → 2 unit tests + the e2e's BOT_STOPPED assertion; the run loop's exit → the e2e's one-tick assertion, 29s instead of 0s). Real-GUI round trip captured: button wrote source="gui", chip went red with a headline byte-identical to `kalshi bot status`, RESUME cleared it and both went green. Surprises: (1) the honesty hole the issue's literal green/amber/grey list would have left — a killed bot's LAST ledger row is its own refusal, so it reads FRESH; without a 4th STOPPED state taking precedence the chip sits green on a bot that is not bidding, which is why red STOPPED was added beyond the issue text; (2) `kalshi bot status --json` silently printed human text because --json is a GLOBAL flag consumed before the subcommand — a trailing --json was landing in argv and being ignored, so status now refuses unknown options instead of dropping them; (3) `grep -c` exits 1 on zero matches, so the shell idiom `[ -f f ] && grep -c p f || echo 0` prints TWO numbers and every arithmetic test after it dies with "integer expression expected".
- 2026-07-24 13:14:21 issue #129 PR #133: needs-human-review — left for operator
- 2026-07-24 13:14:22 iteration 5: building issue #130 (log: finn/logs/20260724-131422-issue-130.log)
- 2026-07-24 issue #130 BLOCKED (no PR): Kalshi bot rung 5 (micro-live behind the human arm) hard-depends on two unmerged `needs-human-review` PRs — #133 (rung 2 gate: `KalshiBotGate.{h,cpp}`, the writer of `kalshi-bot-gate.json`) and #135 (rung 4 kill switch: `KalshiBotRuntime.h` + the stop-check at the top of `decide()`). Both verified absent from a freshly fetched `origin/main`; `KalshiBotDecision.h` there still documents "this rung has no live mode at all". Three of #130's five criteria name those artifacts directly. No honest route around it: reimplementing the gate reader is scope creep AND recreates the exact divergence rung 3 avoided (the BOT panel reads the gate's published verdict rather than re-deriving it); stacking on the PR branches violates branch-fresh and binds rung 5 to contracts that may still move under review; and a partial build is impossible because #133 and #135 both modify the same three files rung 5 must touch (`KalshiBotCommands.cpp`, `KalshiBotDecision.*`, `KalshiBotPanelPresentation.h`), so any rung 5 branch collides with both at merge. Posted the unblock path and put the one genuinely operator-owned choice — wait for the merges vs. authorize stacking — back to the operator rather than picking it. Surprise: the ladder's own ordering was the trap. Rungs 2 and 4 were BUILT before rung 5 but sit behind personal-merge review by design (carve-out condition 3 makes the gate operator-merged by definition), so a loop that picks the oldest `agent-ready` issue will keep reaching a rung whose prerequisites are complete-but-unmergeable — completed work in a `needs-human-review` queue is not the same as landed work, and only `origin/main` counts.
- 2026-07-24 13:18:36 issue #130: no PR after build (blocked or failed) — see log
- 2026-07-24 13:18:38 iteration 6: building issue #131 (log: finn/logs/20260724-131838-issue-131.log)
- 2026-07-24 issue #131 PR #136: Kalshi bot rung 6 — order lifecycle honesty. New pure `KalshiBotOrders` (replay / reconcile / requotable, own file so it collides with none of the in-flight rung PRs): a bid is now an ORDER with `order_state=resting` and a TTL, not a position. Exposure is PR #44's rule verbatim (filled at fill price + remainder at LIMIT price, over everything not canceled/settled), so a resting quote is money the bot counts. Cancels go through an INJECTED canceller and only a confirmed one writes CANCELED_*; an unconfirmed one journals UNCONFIRMED_CANCEL, leaves the order resting, keeps its exposure, and is retried next tick. 21 new tests + 4 panel tests, full suite 130/130, tst_kalshi_bot_orders confirmed RUN as Test #80; six independent neuters each turned exactly their own tests red. Six real CLI transcripts in finn/evidence/issue-131-order-lifecycle.txt, all against a scratch OPENTERMINAL_KALSHI_EVIDENCE_DIR (the operator's live ledger untouched). Surprises: (1) the design's one real fork — calibrator.json carries a MID and no book at all, so a paper fill cannot be measured; the rule is "a resting limit fills only once an observed mid is at or through it, at the limit, never better", stated on every fill row (`fill_model`/`fill_rule`) the way rung 1 stated its own, and rung 1's rows (no `order_state`) replay as `rung1_assumed` positions so a mixed ledger is readable rather than silently averaged — this DOES change the denominator rung 2's sealed gate scores, which is why it is said out loud rather than shipped quietly; (2) ordering is load-bearing — reconcile must run BEFORE decide and the book must be re-replayed in between, or ALREADY_HELD/QUOTE_RESTING blocks every requote the TTL pass just freed; (3) a nested struct's default member initializers are not parsed yet at the point a defaulted argument of the same class is declared, so `decide(..., const Exposure& = Exposure{})` will not compile — it needs an overload; (4) the first TTL e2e accidentally FILLED instead of expiring because the synthetic mids sat exactly on a cent (limit == mid → marketable at once); real mids are continuous (0.0035, 0.8342), so the floored limit rests strictly below the market and fills stay genuinely conditional — synthetic fixtures can flatter a fill model into looking trivial; (5) review caught the one thing eight green neuters could not — the fill check ran BEFORE the settled check, so a resolved market whose stale snapshot still quoted a marketable mid was journaled FILLED_AT_LIMIT and settled into fabricated P&L (rung 1's CONTRACT_SETTLED trap, re-opened by the lifecycle), and CANCELED_MARKET_SETTLED did not feed the settled set, so the resolved contract — no position, no resting order, and never having filled, no settlement row either — was quoted again next tick. Both seen failing first, both closed, both neuter-verified. The test that should have caught them existed and was GREEN: it used mid 0.86 against an 0.83 limit, which makes the fill branch unreachable, so the assertion never reached the code it was named for. A test written with values that cannot enter the branch is not coverage.
- 2026-07-24 14:25:38 issue #131 PR #135: needs-human-review — left for operator
- 2026-07-24 14:25:39 no unhandled agent-ready issues; loop idle-exits at iteration 7
- 2026-07-24 issue #129 PR #135: forward-ported the rung-4 observable-loop PR onto origin/main after rung 2 (PR #133) landed. Merge 35eccdfa (parents 54bf41e3 + 5e4df456); merge-forward, not rebase, so both tips stay ancestors (`git merge-base --is-ancestor` verified for each before push). Two conflicts, both additive-union — main added `kalshi bot gate [seal]`, the branch added `stop|resume|status` — so nothing was chosen between them: both include lines, all six subcommands in kSubcommands/usage()/the pre-config dispatch block, and all three test targets kept. Three deletions from main's side are supersessions, not weakenings: the 5-arg `decide()` (pre-kill-switch form) → the 6-arg one taking `stop`; the `kalshi-bot-decisions.jsonl` and `60` literals → rung 4's shared constants (same values, one source of truth); main's namespace-level `is_event()` kept and the branch's identical local lambda dropped. Weakening was disproven mechanically, not asserted: `git diff` vs the pre-merge tip is EMPTY for KalshiBotDecision.{h,cpp}, KalshiBotRuntime.h, KalshiBotPanelPresentation.h, KalshiScreen.{h,cpp}, all four test files and the plist, and `git diff` vs origin/main is EMPTY for KalshiBotGate.{h,cpp} + tst_kalshi_bot_gate.cpp — the merge edits exactly two files. Full suite 132/132; #79 decision, #80 runtime, #81 gate, #114 e2e kill switch, #120 panel all confirmed RUN by name and number. Kill-switch neuter re-run post-merge (`if (stop.engaged)` → `if (false)`, .o mtime confirmed changed before trusting it): exactly the expected three assertions red — both unit cases plus the e2e's BOT_STOPPED — and nothing else; reverted, rebuilt, 132/132 again. PR is MERGEABLE, needs-human-review kept, NOT merged. Surprises: (1) the test NAME set, not the count, is what proves a CMakeLists union merge — 130 (main) ∪ 131 (branch) must be 132, and captured both name sets from `git show` before resolving so a dropped target could not hide behind a green suite; (2) `ctest ... | tail -40` in a background task truncates the log to 40 lines, so the "confirmed RAN" evidence was gone and the suite had to be re-run capturing the whole thing — pipe to a file, tail the file; (3) the union merge's one real semantic risk was invisible to git — rung 4 made `status` refuse unknown options while rung 2's `gate seal` takes a POSITIONAL JSON payload, so the two could have collided in a shared arg loop; they did not (the strictness is per-subcommand), verified by driving `gate seal '{...}'` through the real CLI against a scratch evidence dir rather than trusting the green suite.
- 2026-07-24 15:11:15 PR #135: MERGED (operator-authorized game-on sequence, CI green)
- 2026-07-24 15:54:38 issue #130: no PR after build (blocked or failed) — see log
- 2026-07-24 16:30:35 no unhandled agent-ready issues; loop idle-exits at iteration 2
- 2026-07-24 16:30:41 iteration 1: building issue #130 (log: finn/logs/20260724-163041-issue-130.log)
- 2026-07-24 issue #130 PR #140: Kalshi bot rung 5 — micro-live behind the human arm. New pure `KalshiBotLive` (openterminal_core) admits a live tick only when EVERY carve-out condition holds at once — human-armed session, bounded in time, published gate verdict PASS and < 1h old, kill switch clear — else rc 4 with a named refusal journaled mode=live carrying NO trading numbers. A permitted bid becomes an intent for the EXISTING prepare_order/submit_order pair with the armed session's own caps (clamped to the charter ceilings); submit_order re-checks all of them and stays sole authority. FAK, because this rung has no live cancel path and nothing may rest unattended. 34 unit cases + 1 e2e + 1 submit-path case in tst_pm_paper; full suite 135/135; fourteen independent neuters each turning exactly its own test(s) red. Real-GUI capture on a scratch profile/evidence dir (operator's app, ledger and live session untouched): [LIVE] badge red, ARMED caps line, LIVE rows with `submit filled` / `submit rejected`. Surprises: (1) live rows landing in the paper ledger had THREE consumers, not one — replay's conditional-mid fill model, settle_paper, and KalshiBotGate::evaluate, which the CLI feeds mode-blind; that last one would have counted live outcomes as the PAPER evidence carve-out condition 3 requires, i.e. the bot grading itself on the trades its own promotion authorised. Both filters went INSIDE replay/evaluate, not at the call sites, because a caller that forgot one would silently invalidate the seal. (2) `session_active` is not `bounded` — a 24/7 arm writes an empty ends_at and the terminal's own activity test (`!ends.isValid() || ends > now`) reads that as ACTIVE and ARMED; boundedness needs its own check or an unbounded arm gets ridden. (3) submit_order's per-contract duplicate guard does NOT cover a filled LIVE order: it counts drafts in ('submitting','submission_unknown','submitted') but a live submit writes the VENUE's state onto the draft ('filled'/'resting'/…), and only the PAPER branch ever writes 'submitted' — so relying on it would have had the bot re-buy the contract it just bought every tick until a cap stopped it. Proved at the real submit path, then closed bot-side with `live_working()`; filed #141 for the shared hand-approved path, which still has it. (4) `--paper --mode live` ran LIVE — `--paper` was taken and discarded, so the launchd job's whole safety story was a flag a later --mode silently beat; silent ESCALATION is the same defect class as silent degradation, now rc 2. (5) the neuter pass again caught a test that could not fail: `a_paper_bid_does_not_block_a_live_tick` used a paper reason code the accepted-only filter already excluded, so removing the mode check left it green — rewritten with a live reason code and mode=paper so only the mode check can catch it.
- 2026-07-24 17:51:02 issue #130 PR #140: needs-human-review — left for operator
- 2026-07-24 17:51:03 loop completed 1 iterations
- 2026-07-24 17:51:54 iteration 1: building issue #137 (log: finn/logs/20260724-175154-issue-137.log)
- 2026-07-24 18:15:39 issue #137: no PR after build (blocked or failed) — see log
- 2026-07-24 18:15:40 iteration 2: building issue #138 (log: finn/logs/20260724-181540-issue-138.log)
- 2026-07-24 issue #138 PR #142: the ADVISOR & CANARY tab now says the v5 duel concluded instead of rendering 32h-old machinery as a live system. Two pure functions BESIDE an untouched `present_advisor_canary` (the issue's "or a wrapper") — `advisor_duel_retirement()` (retired when every advisor write timestamp is silent past 6h) and `present_advisor_duel_archive()` (the frozen record out of advisor_competition_report.json) — so "a restarted loop renders unchanged" is a property of the diff, not a claim a test has to defend. 9 new cases (suite 133/133, Test #119 confirmed RUN); four neuters each turned exactly their own tests red, in BOTH directions (`retired=false` → the 3 retired cases; `retired=true` → the 3 not-retired cases). Real-GUI round trip on a scratch profile seeded with this machine's actual advisor files: banner + frozen record with the armed badge still above it, then one file made fresh → pre-change rendering back within one 5s tick. Surprises: (1) the advisor caught the load-bearing one before I wrote a line — `latest_legacy_live_status_` is NOT an advisor file but the live `kalshi auto live status` poll, so my first plan ("hide the badges when retired") would have covered an ARMED indicator with "DUEL CONCLUDED"; retirement now reads the six files only, the legacy badge is never hidden, and the scratch-profile screenshot happens to show ARMED sitting above the banner — the exact case a unit test had to encode because a no-account profile can't be relied on to produce it; (2) the "final duel record" is a trap for fabrication — result_state is INSUFFICIENT_PAIRED_DATA (53/200 jointly resolved) and the epoch_pair is claude-**v5** vs codex-**v4**, so both "the v5 duel" and any winner derived from paired_brier_delta (-0.0026) would have been half-invented; the banner prints the epochs verbatim and says NO WINNER DECLARED; (3) advisor_competition_report.json is 766KB against a 5s refresh tick — read at most once per session, gated on retirement, or the tab reparses it 720 times an hour; (4) `git checkout -b` from origin/main aborts while the loop runner's uncommitted JOURNAL lines sit on main — stash first, and keep a `git diff` copy in /tmp in case the pop is ever lost.
- 2026-07-24 18:59:58 issue #138 PR #142: MERGED
- 2026-07-24 19:00:00 iteration 3: building issue #139 (log: finn/logs/20260724-190000-issue-139.log)
- 2026-07-24 issue #139 PR #143: the AUTO COCKPIT now says whether its own inputs are fresh. New pure `AutoCockpitPresentation.h` (22 cases, full suite 134/134, Test #122 confirmed RUN; ctest NAME set = previous 110 ∪ exactly one) classifies the cockpit's contract list and ladder book feed live/stale/absent with green/amber/grey, and the header renders on the 1s CLOCK tick rather than inside the 5s ladder engine — which returns early with no selection, so the old header genuinely froze at its last-good text with nothing ageing against it. A not-live cockpit replaces the ladder rows with the stated reason instead of keeping last-good prices; `record_ladder_evidence` skips `render_ladder_surface` in that case so the two owners never fight over the table. Book ages come from `KalshiSurfacePoint::quote_observed_at_ms` on the surface the ladder is drawn from, never a second lookup into `kalshi_books_`. Seen failing first: a stub that always answers live/green turned 12 of 22 red; then three independent neuters each turned exactly its own tests red (never-stale timestamp → 8, always-trustworthy ladder → 9, never-absent books → 1). Deliberately did NOT rebuild the periodic market refetch: that is #137's, whose branch already exists unmerged and touches the same two files — cited on the issue instead, and this header is what makes it visible. Surprises: (1) `quote_observed_at_ms == 0` is unambiguous — BOTH book producers always stamp a time (`KalshiRestClient.cpp:772`, and `KalshiWsClient.cpp:491-493` falls back to now) — so a zero means "no book for this leg", which is what let the silent `quote_for` fallback be counted honestly rather than guessed at; the live capture read 0/188 legs quoted, cross-checked in the ledger (54 of the last 108 surface rows have zero quoted legs, interleaved 1:1 with the operator's 188/188 rows). (2) The label whose text literally says "AUTO COCKPIT" was laid out on the MAKER ORDER tab and the cockpit tab had no header at all — moved, not duplicated, or the screen would have carried two competing headers one of which still freezes. (3) The first real-window capture rendered the new chip as a BLANK BOX: the cockpit tab had no scroll area unlike its three siblings, so the fixed-height pane compressed it to zero height — a header that renders empty is the same "reads as broken" failure the issue is about, and no unit test could see it; the cross-link then vanished for a second layout reason (beside a word-wrapped chip it loses its width). Only looking at the real window caught either. (4) `HOME=` does NOT isolate the Kalshi evidence directory — `evidence_path` goes through `QStandardPaths::AppLocalDataLocation`, which on macOS comes from the process's real home, so the scratch-profile session appended ladder rows to the operator's shared ledger; `--profile` isolates the profile, not the ledger. (5) The machine was locked all session (`loginwindow` at window layer 2004), so full-screen capture returned black and synthesized `CGEventPost` clicks went to the lock window — `screencapture -l<windowid>` still reads a window's own buffer, and the tab had to be pre-selected by a one-line capture-only build that is not in the branch.
- 2026-07-24 issue #139 PR #143 (review round 2, `545570e6`): the reviewer was right and the defect was worse than my first fix admitted — `record_ladder_evidence` wrote the plan summary `cost $… · expected $… · worst $…` UNCONDITIONALLY and only then consulted `ladder_trustworthy`, which guarded `render_ladder_surface` alone. Not a timer race: the notice written by `refresh_auto_cockpit_header()` at :3171 was overwritten 156 lines later in the SAME call, before Qt repainted, so on every not-live pass the header call I had added inside the engine pass was dead code with respect to `ladder_status_`. Fixed by gating all three writes (setText, the green-on-opportunity setStyleSheet, render_ladder_surface) behind one `if (!cockpit_view.ladder_trustworthy) return;`. Two traps the minimal fix would otherwise have kept: (1) the view must be hoisted AFTER :3157-3170, since `auto_cockpit_inputs()` reads `cockpit_ladder_` which only that block repopulates — hoisting to the top gates on the PREVIOUS pass's leg freshness and nothing in the suite goes red; (2) `refresh_auto_cockpit_header` read its own `currentMSecsSinceEpoch()`, a LATER clock than the gate's `now` from :3095 with REST/DB work in between, so a staleness boundary crossed mid-pass reproduces the identical overwrite with both writes nominally gated — it now takes `now` as a parameter, one pass one reading. Suite 134/134, Test #122 RUN, 24 cases. HONEST GAP STATED, not implied: no test fails without this fix and none was added that would — the defect is GUI wiring and there is no KalshiScreen harness; a pure `auto_cockpit_plan_summary(view, plan_line)` helper routing both writers would still pass if someone re-added a bare `ladder_status_->setText(...)` to the engine, so it buys structure not coverage, and the advisor called it correctly as scope creep on a header the reviewer had already verified line by line. Surprises: (1) three figures in my own PR body were false and the reviewer caught all three — "previous 110" ctest names (actually 133), the `timestamp_stale` neuter at 8 red (actually 10), `ladder_trustworthy` at 9 (actually 10); the first two understated because they predated the last two test cases, i.e. a neuter count goes STALE the moment you add a case, and quoting it later is fabrication by staleness. Re-ran all four neuters one at a time, restoring to 24/24 between each, rather than copying the reviewer's numbers. (2) The reviewer's `#include <algorithm>` nit was itself wrong — it has been at KalshiScreen.cpp:79 since 6ac75db2; checked `git show origin/main:` before adding a duplicate, and said so rather than silently skipping it. Never argue more than twice, but do verify before complying.
- 2026-07-24 20:51:23 issue #139 PR #143: merge failed — see log
- 2026-07-24 20:54:54 iteration 4: building issue #141 (log: finn/logs/20260724-205454-issue-141.log)
- 2026-07-24 21:54:16 issue #141: no PR after build (blocked or failed) — see log
- 2026-07-24 21:55:38 loop completed 4 iterations
- 2026-07-24 22:03:25 PR #140: review's screenshot request addressed (evidence-only revision) — awaiting operator merge
- 2026-07-24 22:12:34 issue #139 PR #143: MERGED (loop-approved; retried through network outage)
- 2026-07-24 issue #137 PR #144: resumed an abandoned branch — the 15-minute contract auto-roll. Treated both inherited commits as unverified: merge-forward onto origin/main fe2ac0cd (merge 3e47dc2f, clean), then proved non-weakening mechanically rather than asserting it — the added/removed LINE SETS of HEAD vs the branch tip are identical to main's own change, and HEAD vs origin/main identical to the branch's own change, for KalshiScreen.cpp, KalshiScreen.h and tests/CMakeLists.txt; ctest NAME set = main's 134 ∪ {tst_kalshi_market_roll} = 135, as sets not counts. Suite 135/135, Test #124 RUN, 24 cases (the inherited commit message said 25 — measured, not copied). Six neuters one at a time, restored to 24/24 between each: in-flight guard 2 red, is_expired-always-false 10, preservation branch 3, is_selected_contract 3, stall escape 1, close grace 1. Surprises: (1) the fix's real hinge is invisible to every unit test — the roll only reaches the DOM and charts if setCurrentRow re-enters select_market, and a QListWidget that did NOT reset its current row on clear() would make a roll onto the same row index a silent no-op, i.e. the reported bug surviving a green suite; proved with a scratch Qt program replaying populate_markets' exact clear/addItem/setCurrentRow sequence (clear emits -1 → early return, then every setCurrentRow re-enters). (2) git merged 275 lines of #142/#143 into the same functions cleanly, but ONE placement was semantic: #143's `cockpit_ladder_ = AutoCockpitInputs{}` landed after this branch's teardown-skip, which is correct (reset book ages on a real contract change, keep them when a background refresh re-lists the same one) — a textually identical merge on the other side of that early return would have zeroed freshness every 30s. (3) OPENTERMINAL_KALSHI_EVIDENCE_DIR is the env var that actually isolates the Kalshi ledger; #139 learned the hard way that HOME does not, and it is right there in ServeCommand.h:204. (4) The capture failed and the failure is the finding: machine at the login screen again, the Prediction Markets dock panel would not stay materialised (state=spawning, has_live_widget=false), a QT_QPA_PLATFORM=offscreen run never reached 'Application ready', and the network was degraded (11.5s for one Kalshi REST call). One real 15-min-cadence fetch was captured via a capture-only `cadence_ = fifteen_min` build (not in the branch); no rollover was hand-verified and the PR says so. (5) My own restarts rotated away the earlier session's log, so its 30s-cadence figures became unverifiable — described in the evidence note, deliberately NOT re-quoted as evidence.
- 2026-07-25 issue #137 PR #144: REVIEW — loop-changes-requested. Criteria 2–4 (auto-roll, selection preservation, no-storm) verified independently: rebuilt tst_kalshi_market_roll from the PR head standalone against Qt (26 passed / 24 cases), then ran five neuters one at a time restoring to 26/26 between each — preservation branch 3 red, in-flight guard 2, close grace 1, stall escape 1, is_expired-always-false 10, matching the PR's table exactly rather than being taken on trust. Also checked by hand that Kalshi.fetch_category.markets/.events are real request_error contexts (KalshiRestClient.cpp:500,632,642), so the in-flight release is not dead code, and that has_selection_ is only set true after the new short-circuit. Charter clean: no frozen file, no order/arming path (the price_->hasFocus() guard reduces stray ticket writes), non-goals held, no new CLI TU. Blocked on criterion 5's second half — "manual evidence across a real 15-min rollover" — which the PR itself says was never captured. Asked for two minimal things: a log line carrying decide_market_refresh's reason, and one live observation of a second list fetch ~30s after the first. Surprises: (1) KalshiScreen.cpp:1956 is the screen's FIRST use of isVisible(), and there is no hideEvent, so isVisible() is the sole suppressor of a tick once the timer starts — no prior working path on that predicate to argue by analogy from; the fix is not inert, though, because market_list_timer_->start() is only called from showEvent, so it starts visible by construction, which is what rules out last session's "the screen went not-visible" reading in favour of "the panel never materialised and nothing was exercised". (2) Nothing in the running process reports which refresh branch was taken, which is exactly why that ambiguity could not be resolved from the logs — hence change request 1. (3) zsh does not word-split unquoted variables, so a flags-in-a-variable build helper silently passed one giant argument and every neuter "ran" as COMPILE FAILED; caught only because the harness checked for the binary before trusting a result. Worth keeping: never trust a neuter that did not print a Totals line.
- 2026-07-25 01:42:43 issue #137 PR #144: not auto-merged (labels: needs-scope-approval loop-changes-requested ) — see log
- 2026-07-25 11:00:02 filed BOT-panel fail-open-[LIVE] issue
- 2026-07-25 11:48:39 filed: https://github.com/Trucker2827/Open-Terminal/issues/146 (BOT COCKPIT (Kalshi))
- 2026-07-25 12:52:14 PR #140: evidence-revision attempt finished — see finn/logs/20260724-evening-recovery.log and the PR
- 2026-07-25 issue #137 PR #144: REVIEW round 2 — loop-approved. Both round-1 change requests met and re-derived rather than read: the two tests added by `4227c5ca` (`every_refresh_branch_names_itself_in_the_log`, `the_log_line_names_both_ends_of_a_roll`) had never been independently neutered, since round 1 built at `700ecc73` which predates them — built the target at HEAD `2ccc62b5` (28 passed / 0 failed), then two neuters one at a time in `MarketRollPresentation.h` (`reason=%2`→`why=%2`; ` -> ` dropped from the selection line), each turning exactly its own test red, binary relinked each time, restored to 28/28 between and after, tree clean. Deliberately did NOT re-run round 1's five neuters — `4227c5ca` does not touch the code they exercise, so that is spend without discrimination. Criterion 5's manual clause settled from primary evidence, not the reply: `kalshi-lifecycle.jsonl` holds 18 `kalshi_screen_contract_roll` rows, all `live_eligible:false`, including real quarter-hour boundaries (`KXBTC15M-…1745-45` close 21:45:00Z → `…1800-00` at 21:46:30Z). Raw cadence log counted directly: 74 `periodic`, 131 `in-flight`, 14 `waiting`, 64 `expired`, ZERO `hidden`, two `ROLLED`, one `vanished` — the honesty rule holding under a real payload (absent-but-not-expired → no record) is worth more than the roll itself. Surprises: (1) the charter says "CI green on all three platforms", but this repo's PR CI is Linux-only BY DESIGN — `regression.yml` triggers on `pull_request`, `build-cpp.yml` is `workflow_dispatch` only — so "two checks green" IS green CI here and holding a PR for absent macOS/Windows runs would be inventing a gate; check the workflow triggers before reading a missing platform as a failure. (2) `refresh()` gained a defaulted `bool background` and is connected to `QPushButton::clicked(bool)` at KalshiScreen.cpp:1556 — Qt binds the signal's bool to that parameter, and it is correct today only because `refresh_button` is not checkable; one `setCheckable(true)` away from an operator click silently becoming a background refresh. A defaulted bool on a slot is a trap worth grepping for. (3) `KalshiScreen::evidence_path()` uses `QStandardPaths::AppLocalDataLocation` while only the CLI reads `OPENTERMINAL_KALSHI_EVIDENCE_DIR`, so the previous iteration's note (3) is WRONG in the direction that matters: the env var does not isolate the SCREEN's writes, which is why those 18 roll rows are in the operator's shared ledger. Neither `--profile` nor the env var isolates GUI evidence writes; worth its own issue. (4) My label POST at 22:42:18 removed `loop-changes-requested` as a side effect (repo automation), so the explicit DELETE 404'd — checked the timeline rather than shrugging, and it also confirmed no commits after `2ccc62b5`, i.e. the review was against the live head.
- 2026-07-25 18:44:27 issue #137 PR #144: MERGED (revision round: log line + live evidence)
- 2026-07-25 18:44:27 issue #137 PR #144: MERGED (auto-roll live)
- 2026-07-25 18:44:29 iteration 1: building issue #141 (log: finn/logs/20260725-184429-issue-141.log)
- 2026-07-25 issue #141 PR: submit_prediction_order's per-contract duplicate guard now sees a FILLED live draft. The guard counted ('submitting','submission_unknown','submitted') — but only the PAPER branch ever writes 'submitted'; a LIVE submit writes the VENUE's state ('filled','partially_filled','resting','accepted'), so a filled live order left a draft the IN-list missed and a second live order on the same contract went through. IN-list widened to every status meaning "the venue has, or may have, an order", including `reconciliation_required` (the venue took it; only the local ledger write failed) — a TIGHTENING of the #39/#44 gate, never a weakening. With the shared guard covering it, PR #140's bot-side suppression `KalshiBotLive::live_working()` and its four tests are deleted, and the live tick now passes `{}` as the book. Full suite 137/137, tst_pm_paper confirmed RUN as Test #39 and tst_kalshi_bot_live as #83; two neuters, each turning exactly its own test red — reverting the IN-list to the old three reddened only the filled-duplicate case, and adding 'rejected','cancelled' to it reddened only the refused/killed case. Surprises: (1) whitelist vs blacklist is load-bearing and not a style choice — the obvious `NOT IN ('rejected','cancelled','submit_failed')` would have caught 'prepared' too, and the bot writes a 'prepared' draft on EVERY tick before submitting, so one gate rejection would have wedged that contract until the TTL sweep expired the orphan; the honest fix refuses more, not more things; (2) `submission_unknown` in the IN-list is dead weight — the venue-freeze check at OrderFlowTools.cpp:892 already rejects EVERYTHING for a venue with an unknown-outcome draft, with a different reason, and it runs first, so a test built on that predecessor would fail for the wrong reason; kept in the list anyway so the guard is true independent of check order; (3) deleting live_working() is a real behaviour trade, disclosed rather than hidden: the bot now re-bids a held contract every tick and is refused at submit_order instead of passing ALREADY_HELD before prepare, so each tick leaves an orphan `prepared` draft until the sweep expires it — noisier, but the refusal now comes from the authority that owns the drafts rather than from the bot's own ledger; (4) the pilot bullet I first wrote was too generous and the advisor caught it — `kalshi auto live prepare-next`'s candidate SQL excludes a market with a live `trade_audit` FILLED row, and separately drafts in ('prepared','submitting','submission_unknown','submitted'), so a RESTING or PARTIALLY_FILLED predecessor is in neither list and audits as neither; a pilot market with an order still working at the venue could previously be prepared AND submitted again, and this PR newly refuses it. That is the charter's own "resting counts as risk", so it is a deliberate tightening on the hand-approved path, now pinned by `a_live_order_still_resting_at_the_venue_blocks_the_contract` — and `resting`, not `filled`, is the widest state in the widened list, because a micro-live evidence draft is submitted at the immutable human limit PRECISELY so it can rest.
- 2026-07-25 19:23:54 issue #141 PR #147: needs-human-review — left for operator
- 2026-07-25 19:23:55 iteration 2: building issue #145 (log: finn/logs/20260725-192355-issue-145.log)
- 2026-07-25 issue #145 PR: the BOT badge now fails CLOSED on a ledger row this build cannot read. The badge is read off the newest TICK whose `mode` is explicitly `live` or `paper`; if any row NEWER than that one is unreadable (unknown `event`, or a decision row whose `mode` is absent/unrecognised) the badge is `[UNKNOWN]` plus the launchctl-kickstart hint, never `[LIVE]`. Freshness moved off `ts_ms`-only onto `kalshi_bot_row_ts_ms()` (ISO `ts` fallback), so a dated row proves a tick whatever its vintage. "Newest" compares timestamps, not file position, because two appenders (launchd loop + hand-run `bot once`) interleave. Recognised paper settlements do NOT invalidate a live badge — a settlement is paper by construction and is not a tick, so treating it as one would mask real money at the exchange (advisor caught this before it shipped). Suite 137/137; 6 of 7 new assertions seen red under a behaviour-neuter (badge latch, unknown-mode, ISO freshness, hint, ts-order, plus the runtime unit); real-GUI witness on a scratch profile + scratch evidence dir: amber chip reading `[UNKNOWN] BOT STALE · … · bot binary older than GUI — launchctl kickstart -k gui/$UID/org.openterminal.kalshi-bot`. Surprises: (1) the issue's stated cause (b) is WRONG and the PR says so — the production ledger is 32,561 rows, 100% `mode=paper`, every row carrying `event`, `ts` and `ts_ms`, so no old-schema row could have produced that badge; the only gap in the file is 16:03Z 07-24 → 04:34Z 07-25, i.e. the "15h ago" the operator saw was an HONEST reading of a genuinely dead loop (cause (a)), and the [LIVE] half of the screenshot is unexplained by any code on main. Fixed the fail-open the criteria describe anyway, since it is real: a live tick followed by rows of an unknown vintage kept the badge on LIVE. (2) The first neuter attempt was a false pass — reverting the two headers made the tests fail to COMPILE, so ctest ran the stale binaries from the previous build and printed 42 passed; the real neuter keeps the new API and reverts only the behaviour. Never trust a neuter whose build printed errors. (3) `first process whose unix id is <pid>` in System Events matched the WRONG OpenTerminal instance and moved the operator's window; the reliable discriminator is the window title, which carries the profile name (`Open Terminal [finn145] — …`). Restored it before continuing.
- 2026-07-26 issue #146 PR: the BOT COCKPIT — a decision-rain scene over the bot's own evidence, reachable from the BOT tab and suggested while the loop runs. The load-bearing decision was taking `KalshiBotPanelView` as an INPUT rather than re-reading the ledger: state, mode and the kill switch therefore come from the classifier `kalshi bot status` renders from, so "an all-paper ledger can never render the LIVE mood" is a property of the wiring, not a test. Same reflex on every number: the KPI strip is `kalshi-bot-gate.json`'s own `ledger` block verbatim (settled/W-L/net/drawdown, cap from `params`), the gate node prints the BOT panel's own `criterion_line`, and the freeze bound is `KalshiBotDecision::Config::max_report_age_ms` — frozen rain means "the bot would refuse this report too", not a threshold the scene invented. 30 cases, suite 138/138, ctest NAME set = main's 137 ∪ exactly {tst_kalshi_bot_cockpit}; ten neuters one at a time, restored to 30/30 between each, each turning exactly its own tests red (freeze 3, mood-ignores-live 2, motion-always-true 4, absent-gate-scores-zero 2, ignitions-per-ticker 1, missing-mid-reads-zero 1, silent-column-cap 1, always-suggested 1, span-as-age 1, net-not-coloured 1). Real-GUI capture on a scratch profile whose evidence dir was a 3s mirror of the operator's real files, twice: PAPER moving, then the mirror stopped and the same session frozen — every column tagged FROZEN, scene greyed, census stating the age. Surprises: (1) I drove the WRONG APP for fifteen minutes — a leftover instance from 18:46 was still running out of the same build dir, `System Events … process "OpenTerminal"` picked it, and the cockpit button "missing" from its BOT tab was simply a binary two hours old; the title bar said `[finn146]` on mine and not on it, and I read past that twice. Address windows by `unix id`, and check `ps -o lstart` against the binary's mtime BEFORE concluding a feature did not render. (2) `strings` finds nothing in a Qt binary — QStringLiteral stores UTF-16, so "is my change in this build?" needs `data.count(s.encode('utf-16-le'))`; the first check said 0 and was wrong. (3) `QStringLiteral(kSomeConstexprAuto)` does not compile — the macro needs a literal, not a `constexpr auto` — which is why the repo's own path constants are always `QString::fromLatin1(...)`. (4) A QtTest `QSet<QString>(f().begin(), f().end())` over two calls to a QStringList-returning helper is iterators into two different temporaries: SIGSEGV mid-suite, not a failed assertion. (5) `finn/evidence/*.md` is silently gitignored (`!finn/*.md` un-ignores only the top level), so a written-up evidence note vanishes from `git status` — the directory's convention is `.txt`/`.png` for a reason. (6) The advisor caught that my parity evidence was GUI-to-GUI when criterion 3 says "matches the CLI"; running `openterminalcli kalshi bot status` against the same scratch dir on the FROZEN capture is the stronger proof anyway — both readers agreeing on STALE at the same 120s threshold, with the 2-minute offset between the two readings closing arithmetically against the newest ledger row.
- 2026-07-26 issue #141 PR #147: forward-ported the duplicate-guard PR onto origin/main after the BOT badge (#148) and the COCKPIT (#150) landed. Merge 0a5b9163 (parents cde6ec0d + e725a3b6); merge-forward, not rebase, so both tips stay ancestors (`git merge-base --is-ancestor` verified for each before push). ONE conflict, `finn/JOURNAL.md`, and it was not even a choice: main's journal turned out to be a strict SUPERSET of the branch's (the loop runner had committed its own copy of the same 31 lines), `diff` of the two conflict stages showing zero lines only-on-branch — so the union IS main's file, byte-for-byte. No weakening was possible to begin with, and that is provable rather than asserted: the two sides touch DISJOINT file sets (main: cockpit/panel/runtime/CMakeLists + three test files; branch: OrderFlowTools, KalshiBotLive.{h,cpp}, KalshiBotCommands, tst_pm_paper, tst_kalshi_bot_live), so `git diff` vs the pre-merge branch tip is EMPTY for all six branch-owned files and `git diff` vs origin/main is EMPTY for all seventeen main-owned paths — the merge edits exactly one file. The widened IN-list survives verbatim (all eight states incl. `reconciliation_required`), and `git grep live_working -- ':!finn'` on the MERGED tree returns nothing, so acceptance criterion 3's deletion was not resurrected by main's new cockpit code. Suite 138/138; ctest NAME set identical to origin/main's 138 `add_test` names (compared as sets, not counts); the PR's own targets confirmed RUN by name and number — #39 tst_pm_paper (41 cases, all three #141 cases named in the log), #83 tst_kalshi_bot_live (31, the sanctioned 35→31 deletion intact), #116/#117 the two e2e bot tests, plus #125 tst_kalshi_bot_cockpit from main. Neuter re-run post-merge (IN-list reverted to the original three states; `.o` rebuild AND `tst_pm_paper` relink both confirmed by mtime, build printed zero errors before any result was believed): 39 passed / 2 failed, exactly the two duplicate-direction cases red and the negative-direction case still green, and the failure text is the whole point — the second live submit for a FILLED contract returned `"filled"`, not `"rejected"`. Restored byte-identical (`git status` clean), rebuilt, 138/138 again. Surprises: (1) a forward-port whose two sides share no source file is not a conflict-resolution job at all — the real work is proving the disjointness mechanically and re-binding the guard, and the empty-diff pair is a stronger claim than any prose review of the merged code. (2) The journal conflict looked like the usual additive-union and was not; checking whether one stage is a superset of the other BEFORE hand-merging saved inventing a resolution for a file that had none. (3) Not merged — #141 stays `needs-human-review` for the operator, as the loop left it.
- 2026-07-25 21:20:00 issue #146 PR #150: review left no verdict (session died?) — retrying review once
- 2026-07-25 21:31:35 issue #146 PR #150: MERGED
- 2026-07-25 21:31:37 no unhandled agent-ready issues; loop idle-exits at iteration 4
- 2026-07-25 22:00:00 issue #141 PR #147: MERGED (forward-ported; standing order)
- 2026-07-26 scout (predictions): filed #152 (ledger rotation destroys the record), #153 (no denominator, no pace), #155 (`kalshi bot status` hardcodes mode=paper). Audited the live evidence dir rather than the code alone, and the numbers are the whole story: kalshi-bot-decisions.jsonl is 38.1 MB / 63,305 rows over 46.8 h, growing 0.81 MB/h, so `append_jsonl`'s 64 MB rotation (KalshiEvidenceEngine.cpp:386-399 — it DELETES `.1`, then renames) fires in ~32 h, and every whole-record reader (tick replay, gate) reads the base path only: exposure would read $0.00 with real resting orders out, resolved contracts would be re-quoted, and the sealed gate would silently re-score settled_bids 4 -> 0 and publish that as an ordinary FAIL *with numbers* (its tampered path is the opposite — refusal, no numbers). Funnel over the same record: 304 bids (159 REQUOTED), 288 CANCELED_TTL (95%), 5 fills (1.6%), 4 settlements = 2.05/day, so the sealed 300-settled gate is ~144 days out and the operator's rung 5 is not reachable this quarter at this pace — and no surface anywhere states the denominator or the rate (the cockpit's KPI at BotCockpitPresentation.h:665-683 is decisions/minute, a different measurement). Both #152 and #153 explicitly non-goal the sealed params file: a build agent handed "144 days" will reach for `min_settled_bids`. Surprises worth keeping: (1) the limit is `floor(mid)` (KalshiBotDecision.cpp:209-214) and a fill needs an observed mid at-or-through it (KalshiBotOrders.cpp:277-296), so paper fills are selected by the market moving *down* to the quote — the record the gate will eventually score is adversely selected, disclosed only in a code comment; (2) `--json kalshi bot status` returns `"mode":"paper"` from a literal (KalshiBotCommands.cpp:685) while the GUI badge derives it fail-closed (KalshiBotPanelPresentation.h:304-328), so KalshiBotRuntime.h's "two renderers, one truth" already has a second truth in it; (3) I dropped a fourth candidate — fill inference off the terminal's own recorded Kalshi books — because kalshi-ws-engine.json reports book_update_age_ms ~59.6 min, book-events 230 min stale, trades 656 min stale and last_process_result "planner timed out" (exit 9): the feed is the operator's launchd territory, and an issue built on it would have had no fresh input to consume; (4) github's API TLS-timed-out repeatedly and the retry loop created #154 and #156 as duplicates of #153/#155 — both closed with a comment saying so, and body lengths of all three live issues were verified against the source files (character vs byte counts differ only by the em-dashes).
- 2026-07-26 11:26:12 predictions scout finished — see finn/logs/
- 2026-07-26 11:39:43 iteration 1: building issue #151 (log: finn/logs/20260726-113943-issue-151.log)
- 2026-07-26 issue #151 PR: the ADVISOR & CANARY tab comes off the Kalshi tab row and the concluded v5 duel's record moves to a CONCLUDED DUEL (v5) card on a new ARENA tab — which is also where the arena context strip that used to live INSIDE the advisor tab now lives, since deleting the tab would otherwise have orphaned it (stated as an assumption in the PR, not smuggled in). The load-bearing bit is that `retired` alone cannot drive the card: `advisor_duel_retirement` fails closed TWICE (absent files and a future-dated write both return `retired == false`), so "not retired" spans both "the loop is writing again" and "there is nothing to read", and reading the second as a resurrection would have put the dead tab back on a machine that never ran the duel. Hence a third state — `advisor_duel_presence` → Archived / LiveAgain / NoEvidence, with LIVE AGAIN claimed only on positive evidence of a recent advisor write, and `AdvisorDuelRetirement` gaining a `skewed` field so the skew case is distinguishable at all. Suite 138/138, Test #121 RUN with 20 passed / 18 cases (12 pre-existing untouched, 6 new); ctest NAME set identical to main's 138 (tests/CMakeLists.txt diff vs origin/main is EMPTY). Five neuters one at a time, each rebuilt with zero compile errors and restored to 20/20 between, each turning exactly its own test red. Surprises: (1) I destroyed my own uncommitted header with `git checkout -- $H` as a neuter "restore" — the file was dirty relative to HEAD, so the restore reverted the FEATURE, and the two neuters that followed ran the STALE binary and printed a plausible 19/1 that meant nothing. The tell was in the log I was filtering for "Linking": the build had printed `use of undeclared identifier 'present_concluded_duel_card'`. Commit BEFORE neutering, and treat "compile errors present but a Totals line printed" as a false result, never a pass. (2) The old null guard at the top of `refresh_advisor_canary_status()` (`if (!advisor_system_status_ ...) return`) would have silently skipped the new card's text once the panel's labels stop existing — the same dead-with-respect-to-the-widget shape as #139; the read now happens once and the guard moved down to cover only the panel's own labels. (3) `indexOf` returning -1 for a page the row does not hold makes `-1 + 1 == 0`, i.e. `insertTab` at the FRONT, which would slide the cached `bot_tab_index_` off by one — guarded on `arena_at >= 0` even though `center_tabs_` provably is the ARENA row. (4) The "7-file manifest" the issue asks for is NOT in advisor_competition_report.json; it is the length of `competition_report.py`'s `SCORING_INFRASTRUCTURE` tuple, which is also the charter's frozen-file list — counted from source at capture time and given a provenance comment rather than printed as a bare 7. (5) Criterion 5 is UNMET and the PR says so: the machine is at the lock screen, `screencapture` returns it, and `CGWindowListCopyWindowInfo` reports zero OpenTerminal windows even for the operator's own pre-existing instance 99428 — which is what proves it is the session lock and not the diff. Declined to fake it with a throwaway Qt program re-rendering the two labels: that is a second renderer of the same card and shows nothing about the tab row, which is the primary change. The seeded `profiles/finn151/daemon/` dir is left in place with a one-command repro.
- 2026-07-26 15:59:14 issue #151: branch pushed (builder hung on dead network; work was complete locally)
- 2026-07-26 issue #151 review round 2: the reviewer requested exactly one thing — the criterion-5 screenshot, now that the console is unlocked. Captured on the shipped 17:26 branch build, pid 51208, `--profile finn151`, at the autosaved 1726x1032 (the reviewer's own geometry, not resized to make the card fit): `finn/evidence/issue-151-arena-card-archived.png` shows the tab row with no ADVISOR & CANARY, ARENA selected, and the whole CONCLUDED DUEL (v5) card in ARCHIVED state reading character-for-character STATE 1 of the evidence note. The reviewer's open question — is the card clipped, or was that their window? — measured and settled: the ARENA pane's scrollbar handle is 276 px of a 334 px track, so the content is ~70 px taller than the viewport and the pane opens ~30 px down; the CONCLUDED DUEL card fits whole, but not simultaneously with the `ALPHA ARENA · BLIND FORECASTING CONTEXT` section title. It scrolls normally — one click on the track above the handle paged it to the top (second image). Not fixed and not filed: the reviewer declined to file it and it is outside the issue's criteria. Surprises: (1) the wheel-steal half of the reviewer's report could NOT be tested — the automation here clicks and types but cannot synthesise wheel events, so it is reported untested rather than working. (2) Driving the tab row by coordinates is a trap: the selected tab renders wider, so the SAME x hits BOT when BOT is selected and ARENA when RULES is. Two clicks that "should" have worked hit BOT and floated the cockpit window over the pane. Select a far tab first, then the target. (3) No source file changed this round, so the suite was not re-run — re-running it would have proven nothing about a screenshot.
- 2026-07-26 issue #152 PR: the bot decision ledger stops being a two-generation window and becomes a record — `append_jsonl` grows a `Rotation` policy, the bot ledger uses `KeepAllGenerations` (rename to `max(existing)+1`, never a first free slot, never over an existing name), and every other evidence file keeps the recycling window (pinned by its own test). The naming scheme lives ONCE, in `KalshiBotRuntime.h` beside the readers, and `KalshiEvidenceEngine.cpp` includes it to write generations — a writer with its own copy of the scheme is the exact split this issue is about. One shared reader yields rows oldest-generation-first and all three whole-record readers (tick replay, stopped-tick book, gate) plus one `journal_ledger_row` writer go through the pair, so the policy cannot be forgotten at a call site. Three decisions worth stating: (1) the next generation is `max+1` rather than the first gap — backfilling would put the newest chunk under the oldest name AND heal the hole the gate refuses on; (2) `evaluate()` takes a REQUIRED `RecordIntegrity` (no default) for the same reason the live-row filter lives inside it — a default meaning "the record is whole" is the forgettable direction; (3) refusals carry the truncation anchor forward (`record_anchor_first_settled_ts_ms`), because `gate_command` writes its verdict OVER `kalshi-bot-gate.json` — without the carry-forward the first refusal erases `ledger.first_settled_ts_ms` and the next run scores the remainder, which makes the whole check a one-shot. Tested at both levels: 13 cases in the new `tst_kalshi_bot_ledger_generations` (real `decide()`/`reconcile()` rows, 64 MB reached by growing the file, the base-path-only reader shown reporting $0.00 for a record with $1.66 resting, and the reversed concatenation asserted NOT to produce the filled book) + 6 in `tst_kalshi_bot_gate` + a new e2e over the real `openterminalcli --json kalshi bot gate` (same five ledger numbers across a rotation; hole → exit 3 with no `ledger` block; oldest-row-after-anchor → exit 3; restored record → the original numbers again, so a refusal is recoverable rather than a wedge). Suite 140/140, both new tests confirmed RUN by number (#82, #119) in the ctest log; ten neuters one at a time, each rebuilt and each turning exactly its own tests red — and one that did NOT: neutering hole DETECTION left `tst_kalshi_bot_gate` green because the gate unit test injects `RecordIntegrity` directly, so that path is proved by the e2e and by a second neuter of the gate's own `missing_generations` branch. Widened one thing deliberately and said so in the PR: an EMPTY record with a positive published anchor is refused too — the literal "oldest row is newer" cannot fire when there is no row, but publishing `settled_bids: 0` against a record of 4 already-scored settlements is the same dishonesty. Surprises: (1) I repeated iteration #151's own journalled mistake almost verbatim — my neuter harness "restored" files with `git checkout -- <path>` while the feature was still UNCOMMITTED, and silently reverted two source files mid-run; the tell was the NEXT neuter failing to build with `out-of-line definition of 'append_jsonl' does not match any declaration`. Commit first, then neuter, and treat a build error in a neuter run as "this result is void", never as a pass. (2) `python3 - <<'PY'` cannot also read data from a pipe — the heredoc IS stdin, so the e2e's JSON summariser had to be written to a file first; and a python heredoc that itself contains the line `PY` terminates the OUTER shell heredoc early, which is how a "parse error near `}`" appeared in a file that had no such error. (3) The operator's dir today has no `.1`, so the first legacy rotation deletes nothing: `.1` + base is the complete record and the multi-generation reader repairs it retroactively — that case is a normal verdict, not a refusal, and it has its own test so a future tightening cannot break the live gate by accident.
- 2026-07-26 issue #153 PR: the paper record gets a DENOMINATOR and a PACE. `KalshiBotFunnel::measure()` counts the funnel over the rows it is handed (bids → fills → settlements, resting now, the four cancel reasons) and the paper tick publishes it atomically to `kalshi-bot-funnel.json` from the ledger it had already replayed, so no reader re-reads 45 MB; `kalshi bot status` (human AND `--json`) and the BOT panel both render it through ONE header-inline formatter, which is why criterion 5 is a property rather than a claim — `view.funnel == kalshi_bot_funnel_lines(file, now)` in the panel unit test, and the e2e asserting the CLI's human lines are character-for-character the JSON's `funnel_lines`. The split is load-bearing: struct + JSON + file reader + formatter are header-inline (the GUI does not link the CLI's TUs, and `tst_kalshi_bot_panel_presentation`/`tst_kalshi_bot_cockpit` stay on Qt6::Core alone), while `measure()` lives in openterminal_core because it needs `KalshiBotOrders::replay()` and `KalshiBotGate::seal_valid()`. Three decisions the advisor caught before they shipped and each has a test: (1) the live-row filter lives INSIDE `measure()`, not at the call site, for the gate's own stated reason — `resting_now` comes from a replay that already skips live rows, so a live bid counted here would put the numerator and the denominator in different universes, invisible until rung 5 arms; (2) settlements are deduped by `position_id` with the gate's identity requirements, because `settled_remaining` subtracts from the gate's own minimum and a raw row count would be arithmetic across two denominators; (3) the params SEAL is checked, not just the number read — a tampered params file makes the gate refuse and publish nothing, so pacing toward the minimum it states would quote a target the gate will not honour. Absence is an ABSENT KEY throughout: no bids → no `fill_rate` (while a real 0.0 over real bids IS published, both directions pinned), span under an hour → no pace, zero observed rate → no pace, no `inf` and no `0 days` anywhere. Suite 142/142; ctest NAME set = origin/main's 140 ∪ exactly {tst_kalshi_bot_funnel, e2e_kalshi_bot_funnel}, nothing dropped; new tests confirmed RUN by number (#83, #121). Eleven neuters one at a time, feature COMMITTED first, each rebuilt with zero `error:` lines and each turning exactly its own tests red (fill_rate always-present 2, live-rows-counted 1, settlements-raw 2, no-hour-floor 1, zero-rate-projected 1, seal-unchecked 1, resting-guessed 1, stale-age-dropped 1, panel-stops-rendering 2, tick-publishes-nothing = the e2e, assumed-fill-count 1). Surprises: (1) criterion 4's stated observable is now WRONG in the issue's own direction and the PR says so — measured on a read-only copy of the operator's evidence (kill switch thrown in the scratch dir first, so the stopped path takes no venue action and does not sweep 55 h of expired TTLs), the record reads 304 bids / 5 fills / 1.6% / 4 settled exactly as specced, but the span has grown 46.8 h → 2.3 d with settlements still at 4, so the rate is 1.73/day and the gate is ~171 days out, not ~144. The pace got worse; the arithmetic is the same. (2) 9 of those 304 bids carry NO `order_state` — they are rung-1 rows, and `replay()` books them as ASSUMED fills at the quoted price while stating no `fill_model` of their own, so the FILL MODEL line would have described a record holding 9 assumed fills as purely rung6_conditional_mid. Added `legacy_assumed_fill_bids` as its own count (not folded into `fills` — nothing observed those fill) and a clause on that line; this is the one thing here that is more than the issue asked for, and it is the difference between an honest line and a misleading one on the operator's actual record. (3) `resting_now 0` looked wrong against 9 bids with no terminal row and was verified by re-implementing replay's semantics in python over the real ledger — the 9 are exactly the rung-1 rows, replayed as filled, so 0 is correct. Do not publish a count that surprising without an independent derivation. (4) The `fill_rule` text on disk is MOJIBAKE — `KalshiBotOrders.cpp:291` writes a UTF-8 em-dash source literal through `QString::fromLatin1()`, so every fill row since rung 6 carries `â` where `—` belongs. My renderer prints the ledger's own value verbatim (which is what criterion 4 demands), so it faithfully reproduces the corruption; the writer is the bug, it is adjacent code, and it is filed separately rather than fixed here.
- 2026-07-26 issue #155 PR #162: `kalshi bot status` stops reporting a mode from a constant. The word was a second truth inside the very header that exists so the chip and the CLI "must never be able to disagree" — the GUI badge derived it from the newest readable tick and failed closed to UNKNOWN (#145), while the CLI's copy was the literal `{"mode","paper"}`, so a bot ticking `mode=live` read LIVE in the window and paper in the shell, and the human renderer printed no mode word at all. The newest-readable-tick rule, its fail-closed UNKNOWN, the row-ordering and the `[MODE] status` form all moved to `KalshiBotRuntime.h` as `kalshi_bot_mode()` / `kalshi_bot_mode_headline()`; the panel keeps NO copy, which is why every #145 assertion passes with the assertion text unedited (the only edit to that test file is one added case). `is_live(row)` deliberately stayed in the panel — the per-row LIVE marker in `decision_line` is a different rule from the newest-tick classifier, and merging them would have changed decision-line rendering for no reason. Absence is an ABSENT KEY: a record where nothing states a mode carries no `mode` at all, the same rule `last_decision_age_ms` follows, and prints no badge rather than an invented `[PAPER]`. Suite 143/143; ctest NAME set = origin/main's 142 ∪ exactly {e2e_kalshi_bot_status_mode}, nothing dropped; targets confirmed RUN by number (#81, #122, #128, #130). Nine neuters one at a time, feature COMMITTED first, each rebuilt with zero `error:` lines and restored to 4/4 between — and the two that matter are N1 (classifier always answers `paper`) turning the panel test AND the e2e status test red together, which IS criterion 4's stated property, and N2/N3 (the CLI's literal key, the bare headline) turning ONLY the e2e red, i.e. the two regressions this issue names are gated by the shipped-binary test and by nothing else. Surprises: (1) declined to relocate `status_summary` even though it is the object criterion 4 names — it is anonymous-namespace in a TU only `openterminalcli` compiles, and moving it (with its #153 funnel block) into the shared header to make it assertable in-process would have been a bigger diff than the classifier itself; the e2e over the real binary, the precedent #153 set, proves the SHIPPED object directly and is smaller. (2) The validity rule is `index >= 0`, NOT `ts_ms > 0`, and that is load-bearing in both directions: an UNDATED tick still states its mode (dropping it would let a mode nothing claimed win by default), while a record of DATED paper settlements claims nothing at all. So the issue's phrase "no dated tick at all" is looser than the implemented rule — the rule is "neither stamp is valid" — and the PR says so; the e2e's no-tick fixture is settlements-only for exactly that reason. Tidying `valid()` while moving it would have silently broken #145. (3) Two of the nine neuters leave the e2e green (N6 unstated-mode-reads-paper, N8 newest-by-file-position) because its unknown fixture uses an unreadable *event* and its rows are appended in time order; both properties are pinned by the runtime and panel unit tests instead, and that is stated in the PR rather than papered over. (4) The advisor caught that `an_undated_tick_still_states_its_mode` was the one new assertion no neuter had reddened — N9 (`valid()` also requiring `ts_ms > 0`) is the one that does, and running it was the difference between "eight neuters" and criterion 5. (5) Getting off `main` needed a stash: the loop runner's own two uncommitted journal lines blocked the checkout, and they are the runner's to commit, not this PR's — set aside, branch, restored unstaged, and this entry committed alone as `docs(finn):` the way #141 and #146 did.
- 2026-07-26 issue #158 PR #163: two-tier quoting — the bot now CROSSES the spread when the edge still clears `spread_cost + taker_fee + cross_margin` (default $0.02), instead of only ever resting at `floor(mid)` and waiting for the market to come down (151 rested / 147 canceled / 4 filled over rung 6's first day). The hurdle is journaled in both algebraic forms — `side_edge > spread + fee + margin` and its rearrangement `side_p − cross_price − fee > margin` — so the tier choice is checkable from the ledger alone, on rows of BOTH tiers (a rest priced against a real book shows the sum it failed). Load-bearing decision: the tier FAILS CLOSED twice over. No ask for the side being bid → `REST_NO_BOOK`; an ask below the mid it is supposed to be half of, or one rounding to a whole dollar → `REST_BOOK_INCONSISTENT`. The second guard is the one that matters: `spread_cost = ask − mid` going negative would make crossing look CHEAPER than resting, i.e. a fabricated free lunch, and inconsistent data is a species of unavailable. Advisor flagged it before any tier code was written; tracing `yes_mid` to `ServeCommand.cpp:1222` then showed it is `(yes_bid + yes_ask)/2` off the SAME normalized book as the `execution` block, and that a book missing either side yields `yes_mid = 0` which the calibrator already skips — so the guard is belt-and-braces on the YES side and load-bearing on the NO side, where Kalshi's NO book is a separate book (the NO ask is read directly, never as `1 − yes_bid`; the test's two books are deliberately non-mirrored so an inference would give a different price). Fee judged per contract at n=1 because the schedule ceils to the whole cent for the whole ORDER and the size the caps allow cannot be known before the tier is; a one-contract ceil overstates the hurdle, which is the safe direction. Ask rounded UP to the cent — paying is a cost, and rounding a cost down flatters the hurdle. Fill model untouched: a crossing bid still fills only against an observed mid at or through its limit and still fills AT its limit, never at the mid; bid rows now state `fill_model`/`fill_rule` by name (a .cpp-only include of KalshiBotOrders.h, so one literal, no drift). Book passthrough added to `spot_calibrator.py` deliberately OUTSIDE `FULL_FEATURES`/`MARKET_FEATURES` — those tuples round-trip through the saved model state, so adding to them would retrain nothing and invalidate everything. Suite 143/143; ctest NAME set = main's 142 ∪ exactly `{e2e_kalshi_bot_crossing}`, none dropped. Six neuters, each rebuilt and re-run with the `.o` rebuild confirmed: tier disabled → 5 decision tests red + e2e red at part 2; fee dropped from `net_ev` → the fee-flip case red; inconsistent-book guard removed → the fail-closed case red; panel and cockpit `quote_style` suppressed → one each. New e2e contrasts two contracts with the SAME p (0.98) and the SAME mid (0.835) differing only in spread, plus a control with the book fields stripped that rests both and fills neither — so the fill is attributable to the crossing and not to the fixture. Surprises: (1) the criterion's "filled position in one tick" is not literally reachable and I did not engineer around it — `run_tick` is replay → settle → reconcile → decide with ONE lifecycle pass, so an order placed in tick N is observed in tick N+1; observing the market after the order exists is the honest sequence, and adding a second reconcile would be tick-orchestration scope creep. Said so in the PR under that criterion rather than quietly satisfying it. (2) A resting bid at `floor(mid)` DOES fill under the rung-6 model whenever the mid is already whole-cent (`mid ≤ limit` is satisfied at equality) — the funnel's 4 fills are exactly that case, and the e2e's thin contract needed a half-cent mid (0.835, which is what real `(bid+ask)/2` produces) to rest at all. (3) `decide()` is shared with `run_live_tick`, so crossing changes what a live-armed session would PAY with zero live-path code changed — every submit-time gate and cap is untouched and re-checked from the immutable draft, so it is not an invariant breach, but it IS a real behavioural change to live pricing; labelled `needs-human-review` rather than left to auto-merge. (4) Safe rollout falls out of the fail-closed rule: until the operator bounces the calibrator job, no report carries book fields, every contract takes `REST_NO_BOOK`, and the bot quotes exactly as it does today. Not bounced — the operator's call. (5) The advisor caught a real honesty defect AFTER the PR was open: I had promoted BOTH `fill_model` and `fill_rule` onto bid rows, and `kFillRule`'s prose says "calibrator.json carries no book" (falsified by this very PR), "its market mid is the ask proxy" (false on a cross) and "it selects on the market having moved to the quote" (inverted on a cross — the quote moves to the market). A false disclosure sentence on exactly the rows this change is meant to make the majority: category-1 in prose rather than in numbers. Bid rows now carry the model's NAME only; the absence is asserted and neuter-checked. `kFillRule` is still stamped on FILL rows by KalshiBotOrders, an explicit non-goal here — filed as #164. (6) Then I LOST that fix to `git checkout --` mid-neuter: the neuter had been applied on top of an UNCOMMITTED correction, so restoring to HEAD silently brought the bug back, and the resulting diff looked plausible enough to push. Caught it only by grepping the file for the field. The memory rule is "commit before neutering" and it covers every uncommitted line, not just feature work. (7) The PR's #153 funnel claim was reasoning, not evidence, until I read `KalshiBotFunnel.cpp:108,115` — it buckets on `action` alone (reason_code only splits the three cancel reasons), so crossing bids and fills flow in unchanged; restated with file:line. That read also turned up a second reason bid rows must not carry `fill_rule`: the funnel republishes the LAST one it sees on any row carrying a `fill_model`, so a bid row would have overwritten the fill rows' sentence. (8) The journal has two writers and I committed the runner's line by accident (`git add finn/JOURNAL.md` swept up its uncommitted append); backed out and restored to the working tree unstaged.
- 2026-07-26 issue #158 review round 2 (PR #163): the reviewer asked for exactly one thing and was right about it. I had refused to stamp `KalshiBotOrders::kFillRule` on crossing BID rows because every clause of it is false for a cross, then left it stamped unconditionally on the crossing FILL rows at `KalshiBotOrders.cpp:291` — the reviewer dumped my own e2e's fill row to prove it: `fill_rule` claiming "calibrator.json carries no book" on a row whose order was priced BY that book, "its market mid is the ask proxy" beside an `observed_mid` of 0.835 that was not the ask, and "it selects on the market having moved to the quote" on a quote that moved to the market. Half a fix is a false disclosure on exactly the rows this change makes the majority. `kCrossFillRule` now says what is true of a crossing fill, and `fill_rule` is selected on the order's OWN journaled `quote_style` (the replayed resting order is the whole bid row — `KalshiBotOrders.cpp:215` keeps it) rather than re-derived from prices in `reconcile()`: the decision function already recorded which tier priced the order, and a second derivation is a second truth that can disagree with the ledger. Pre-#158 rows carry no tier and were passive, so they keep the passive sentence. Three clauses of the new prose were written twice: the first draft said a cross "does not wait for the market" full stop, which `reconcile():280` falsifies — the condition is RE-tested against the NEXT tick's observed mid, so a market that moves away, a contract that settles or a report that goes stale still prevents the fill. Replacing one false disclosure with another would have been the same bug. Written ASCII-only on purpose: the neuter run printed `kFillRule`'s em dash as `\u00E2\u0080\u0094`, which is #164's mojibake reaching the ledger through `QString::fromLatin1`, and a new string must not join it. Verified: `a_fill_states_the_tier_that_actually_quoted_it` covers all three shapes (cross / rest / pre-#158) and compares the WHOLE constant plus asserts the other tier's sentence is absent — a one-sided `contains("ask proxy")` at :378 is precisely what let the wrong sentence ride unnoticed. Neutered to `crossed ? kFillRule : kFillRule`, `.o` rebuild confirmed: 22 passed / 1 failed, the crossing case alone. E2e asserts the same on the real ledger row and its `ok:` line was seen in `ctest -V`. Suite 144/144; ctest NAME set = main's 143 ∪ exactly `{e2e_kalshi_bot_crossing}`, none dropped. Rebase was mechanical as the reviewer said — `finn/JOURNAL.md` twice (the #155 line landed between my two journal commits), both writers' lines kept. Residual, not fixed here and said so in the reply: a REST row priced against a real book that failed the hurdle also gets `kFillRule`'s now-conditionally-stale opening premise; the mechanism it describes is still accurate for it, and the reviewer scoped that string to #164.
- 2026-07-26 issue #158 review round 3 (PR #163): the reviewer's one ask, and it is my own knock-on — round 2 fixed the false disclosure on the fill ROW and thereby moved it up a level, into the published funnel. `KalshiBotFunnel.cpp:100-104` was written when one `fill_model` implied one `fill_rule`: `fill_models` is a deduped SET and coped, `fill_rule` was a SCALAR every fill row overwrote. Before #158 no ledger could hold two rules, so it was correct until this PR; after it, a record with one passive fill and one crossing fill published whichever came LAST, and the panel then stated over crossing fills exactly the sentence round 1 established is false for them. The reviewer measured it (two orderings, two different published sentences, same two fills) rather than inferring it, and I reproduced the same swap through the real CLI before and after. The fix is the operator's shape, not the reviewer's suggested set: `fill_rules` keyed by `quote_style`, each key carrying its OWN sentence and its OWN fill count, so a mixed record states both and neither is stated over the other's fills. The key is read off the ledger — `KalshiBotOrders::reconcile()` now carries the order's journaled `quote_style` onto the fill row beside the sentence it selected, so the funnel buckets by the value the deciding tick wrote instead of matching prose. Three refusals to invent one, each with a test: a fill row that names no tier counts under `unstated`, never `rest` (it WAS passive, but it never said so — that is the difference between reading a ledger and completing it); a tier whose rows carry different sentences quotes NEITHER and says they disagree, because first-wins is as order-dependent as last-wins and would have been the same bug a third level down; and every `action: fill` row lands in exactly one bucket including one carrying no rule at all, so the per-tier counts SUM to `fills` — a count that only counted the rows carrying a rule would under-report the record by omission. `QMap` and the published object's sorted keys, so neither file nor rendered line can depend on row order, asserted by comparing the whole `to_json` and the whole rendered line across both arrival orders. Suite 144/144; ctest NAME set unchanged (main's 143 ∪ `{e2e_kalshi_bot_crossing}`, none dropped) — no test added or dropped this round. Four neuters, feature COMMITTED first, `.o` rebuild confirmed each time and each turning exactly its own tests red: within-tier disagreement collapsed to first-wins → the disagree case; every fill forced into ONE bucket → 3 cases (the keying itself, both directions as round 1 taught); the published object reduced to its last entry → the mixed case AND the e2e on the REAL published file, printing the original defect verbatim (`{'rest': {...passive sentence...}}` over a record containing a crossing fill); the fill row's `quote_style` suppressed → the orders case. Surprises: (1) the advisor caught two defects in my plan before a line was written — I had planned to bucket only rows carrying a rule (silently under-summing `fills`) and had talked past the within-key last-writer entirely, which is *literally* the defect one level down from the one being fixed; the sum-equals-`fills` assertion is the one my four enumerated tests would not have caught. (2) It also flipped my call on keeping a legacy scalar `fill_rule` for compatibility: it buys only a smaller test diff, and it keeps alive the exact renderer path — one unkeyed sentence — that the reviewer condemned, leaving me to argue from build history that nothing mixed can reach it. Dropped; the two fixtures that referenced the scalar are the FIRST test deletions in this PR and the reply says so. (3) The PR body's round-1 line "`funnel.fill_rule` (`:103-104`) is untouched" was true when written and false after `966aea9b`; the reviewer asked that it not stay as-is, and a stale self-description is the same category of defect as a stale disclosure — edited in place with a note rather than silently rewritten. (4) No rebase needed this round: `mergeStateStatus` was UNSTABLE (CI pending), not DIRTY, and `HEAD..origin/main` was empty — checked instead of assuming from the previous round.
- 2026-07-27 issue #165 PR #166: bot discipline — an untrusted signal now PASSES instead of papering a labelled bid, and a RESTING quote must clear `edge_threshold + rest_premium_usd` ($0.03 default) while the crossing hurdle is untouched. Both changes are tightening-only: fewer orders, never a different price. Load-bearing decisions: (1) the untrusted refusal is ONE row, not one per contract — trust is a property of the REPORT, so it sits beside `REPORT_STALE`'s refusal, and it returns before a single contract is priced so nothing downstream can reinstate the bid; (2) the trust predicate became a shared `KalshiBotDecision::signal_trusted()` because `KalshiBotCommands.cpp:376,498` each re-read `adds_value_over_market` raw — the moment `decide()` added a condition, a tick that passed every contract as untrusted would have printed `signal TRUSTED`, which is #163's `fill_rule` false-disclosure defect in a new place (advisor caught this before any code was written); (3) that added condition is `brier_available`: the calibrator's ≥100-sample floor stays where it is measured (`spot_calibrator.py:289`, read to confirm rather than inferred from a header comment), but a report asserting value while carrying no Brier is contradicting itself, and unmeasured is not trusted the same way an unknown spread is not a free one; (4) the premium gate runs AFTER the tier is chosen, so a contract that clears the crossing arithmetic but not the resting premium CROSSES — the asymmetry is the point and nothing ever converts a rest into a cross. Suite 145/145; ctest NAME set = main's 144 ∪ exactly `{e2e_kalshi_bot_discipline}`, none dropped. Two neuters, feature COMMITTED first, `.o` rebuild confirmed each time: `if (!trusted)` → `if (false)` turned 3 unit tests red plus the e2e at "a bid was placed on an untrusted signal"; dropping the premium term from `rest_threshold` turned the 2 premium tests red plus the e2e at "the thin resting edge was not refused". New e2e proves the zero-orders criterion AS WRITTEN — `resting_orders` and `exposure_usd` read off the shipped binary's own `--json` tick summary, not inferred from the absence of rows — and contrasts two contracts with the SAME 0.12 edge where the bookless one passes on the premium and the tight-book one crosses. Surprises: (1) the premium is the effective threshold for the WHOLE bot today, not for a minority tier — no report carries book fields until the calibrator job is bounced, so every contract takes `REST_NO_BOOK` → rest tier → the live hurdle is 0.13, the exact inverse of #158's safe-rollout asymmetry; said so in the PR rather than leaving it to be discovered. (2) Raising the resting hurdle moved fixtures across four test files and two e2e scripts (`p_yes_full` 0.95 → 0.98; prices/sizes unchanged because the mid did not move) — the 0.12-edge bid was load-bearing in more places than the decision tests, including `tst_kalshi_bot_ledger_generations`, which only the FULL suite caught. Two #158 tests instead zero `rest_premium_usd` explicitly, because their subject is the crossing arithmetic and zeroing an unrelated hurdle is what they already do for `cross_margin_usd`. (3) Two prose sentences became false the moment change 1 landed — the panel's and `print_tick`'s "every bid is journaled SIGNAL_UNTRUSTED", when there are now no such bids. Corrected despite the issue's "no change required beyond rows": the charter's honesty rule outranks a scope note, and #163 taught that a false disclosure is worse than none. (4) NOT labelled `needs-human-review`, deliberately and with the reasoning in the PR: #163 was labelled because crossing changed what a live-armed session would PAY; this one only removes orders, which is the carve-out's required direction. (5) The advisor's post-PR check found the state this change makes permanent — an untrusted calibrator now yields zero bids indefinitely, where before a bids==0 window was transient — and the funnel formatter turned out to be already guarded: "FILL RATE UNAVAILABLE · this record contains no bid, so there is no denominator to divide by", verified through the real CLI on a scratch untrusted record rather than reasoned from the code. (6) `--rest-premium 0` is refused by `take_double` like every money flag; the e2e uses 0.001 to prove the premium is what refuses the bid, and the parser was left alone.
- 2026-07-27 issue #167 PR #168: the promotion gate now re-evaluates itself on the loop's cadence. kalshi-bot-gate.json was written only by `kalshi bot gate`, so it sat five hours stale while the cockpit rendered its numbers as current — the verdict that decides paper->live depended on someone remembering a command. Every PAPER tick now calls the same writer the CLI calls (`publish_gate_verdict`), handing over the ledger rows it has already read: measured marginal cost ~0.12 s per tick on a 38 MB / 102.6k-row record against a 60 s interval (a manual `kalshi bot gate`, which re-reads the record, is 0.68 s — reusing the rows saves ~0.5 s a tick). Two judgment calls the issue left open. A LIVE tick deliberately does NOT re-evaluate: the verdict is one of the four conditions that admit it to live, and refreshing your own authorisation is self-authorisation whatever the arithmetic says — so the paper loop, which is what the launchd job runs, is the only automatic writer. And the stopped path publishes too, on `publish_funnel`'s reasoning: the record a stopped tick refuses to add to is the record the gate scores, so letting the verdict age the moment the kill switch went on would make the file's age look like the record's. The surprise was in the test, not the code: cadence turns the record-anchor carry-forward (`published_first_settled_ts_ms`, read back out of the verdict this tick is about to overwrite) from a few evaluations a day into 1440, so the e2e runs four consecutive ticks and asserts the verdict, criteria, ledger block and anchor are byte-identical with only `ts_ms` advancing — a ratchet in that feedback loop would surface as a RECORD_INCOMPLETE on tick 2. Display staleness is two tick intervals, and is deliberately not `KalshiBotLive::kMaxGateAgeMs`'s hour: one asks whether the screen is current, the other whether a verdict may admit real money, and a comment now says so in both places so neither gets "fixed" to match the other. Labeled needs-human-review: nothing weakens carve-out condition 3 (the verdict is the same pure computation over paper rows and still flips to FAIL), but what satisfies the one-hour admission bound is now the running loop rather than a human's memory, and that is the operator's call to bless.
- 2026-07-27 issue #169 PR #173: the Kalshi edge autopsy — a research iteration, so the deliverable is `docs/research/2026-07-27-kalshi-edge-autopsy.md` plus five read-only scripts under `scripts/research/`, and nothing in `src/` moved. The verdict the numbers force: the signal is not mistuned, it is uninformative. Over 239 reconstructed contracts the calibrator scores Brier 0.1043 against the raw mid's 0.0989 — worse, and worse in every slice except the deep tails where both are ~0.002. Q3 is the one that closes the door: walk-forward Platt and isotonic make it WORSE (−0.0074 / −0.0096), and a fully overfitted in-sample isotonic fit reaches only 0.0980 versus the mid's 0.0989, so even with perfect hindsight recalibrating this signal merely ties the market price. Four things were load-bearing and none were assumptions. (1) The join is the analysis: bot tickers ⋈ kalshi-settlements.jsonl yields only 81 of 463 contracts (17.5%, and 0% on 07-26) — ID formats match exactly, the settlement feed simply does not cover what the bot watched, so the advisor's instruction to probe join yield BEFORE writing a method section is what kept this from being a report about 81 contracts or, worse, a report that quietly derived outcomes without saying so. (2) Deriving the rest was earned: BRTI's 60-second average at close versus the ticker's strike reproduces Kalshi's own `result` 42/42 and its `expiration_value` to $0.71 mean error (the instantaneous tick errs $13.21 — rejected), which incidentally proves the ticker's close time is EDT rather than assuming it; every derived row is tagged and every headline is repeated on the recorded-only subset, which agrees in sign. (3) Q1 was built model-free on purpose — pricing a Gaussian off fresh spot and calling the distance to the mid an "edge" mostly measures the Gaussian — so the primary statistic is sign(spot move) × (mid at t+h − mid at t) after a move that ALREADY completed, needing no model at all; the Gaussian version is reported second and labelled model-dependent. (4) Splits in Q3 cut the CONTRACT list by close time, never rows: a contract carries up to 56 rows sharing one outcome, and a row-wise split would have manufactured out-of-sample "improvement" out of leakage — the half/half split does show a small gain that the walk-forward then reverses entirely, which is exactly the shape that leak would have faked. Surprises worth keeping: (a) the calibrator's self-report is measuring the wrong thing in three ways at once — `brier_full` stores one pair per OBSERVATION (≤60/contract) so `training_samples: 500` is really ~8–10 contracts (measured from the state file's own `pending` obs distribution: min 2 / median 60 / mean 49.4, so 500 pairs ÷ 49.4 ≈ 10.1 — the advisor caught that my first draft's '8–33' was unsourced and contradicted the script's own printed bound), `adds_value_over_market` gates on ≥100 of those correlated rows (two settled contracts clear it), and `brier_market_baseline` is a TRAINED 1-feature logit on the mid rather than the mid itself, so "beats the market" never meant beating the market; that trio fully explains the flip-flopping around zero the issue describes, and it is a bookkeeping defect, not a modelling opinion (F2). (b) The quote lag is REAL — after a 3σ move the Kalshi mid drifts a further +2.07¢ over the next 15 s (t=2.5) and has caught up by 60 s — but it is almost exactly fee-sized: half-spread 0.53¢, fee 1.55¢, 2.07 − 0.53 − 1.55 ≈ 0. The book is slow and the FEE, not the spread, eats the slowness; that inverts the obvious follow-up from "find lag" to "capture it without crossing". (c) Retention, not capture, is the binding constraint: every log rotates at ~67MB into ONE `.1` that the next rotation overwrites, so BRTI (2.6 s cadence) keeps 116 h while kalshi-tickers.jsonl (0.12 s) keeps 8.0 — the paired window for Q1 is 8 hours and 8 events at 3σ, and the recorder follow-up is therefore about KEEPING a downsampled series, not about starting to record something that already streams (F1). (f) Q1 silently excluded a whole market family until the advisor asked what the `no_strike` filter count was: 59 of the 169 quoted markets are `KXBTC-…-B65050` BAND contracts, and the settlement feed proves it (exactly one $100-wide band per event settles YES, the one containing `expiration_value`). Excluding them is CORRECT — the statistic multiplies a mid change by the sign of the spot move, which presumes YES is monotone in spot, and for a band a big up-move can push spot out of the band and lower its YES — but excluding them under a generic `no_strike` counter was not disclosure. Now counted as `excluded_band_market` with the reason in the code and the report. (d) Q4 is n=15 and the honest answer is mostly "unanswerable": 8 wins / 7 losses but −$7.03, because the winners were bought at a mean 0.7175 and pay a fraction of stake while losers pay all of it — descriptive arithmetic, not a signal to buy underdogs; and the $0.03 adverse-selection premium cannot be sized either way because all 43 fills came from resting quotes and the ledger holds not one settled crossing fill to contrast them against. (e) The read-only discipline needed a specific refusal: `spot_calibrator.run_once()` rewrites the operator's live `spot-calibrator-state.json` and `calibrator.json` as a side effect of reading evidence, so the state file is opened directly and the module is never imported — grep-verified that all five scripts open strictly `"r"`.
- 2026-07-27 issue #170 PR #175: the retained paired series — `kalshi-lag-series/` beside the evidence logs, written by `openmarketterminal-qt/scripts/kalshi_lag_series.py`, BRTI at ≤1 Hz plus per-market top-of-book on change with a 10 s heartbeat, KXBTCD `-T` contracts inside their last hour, bounded by TIME (30 days) and never by size. Four decisions were load-bearing. (1) The retained rows are written in the SOURCE logs' own schemas, so the series reads as the oldest ROTATION of the same stream: `kalshi_edge_common.read_jsonl` prepends it and `q1_quote_lag.py` is byte-for-byte untouched — which is the fifth acceptance criterion taken literally rather than satisfied by editing the consumer. (2) Overlap is resolved in favour of the live log (retained rows are injected only strictly older than the live window's first instant), so the series can never double-count itself; proved on real evidence, where q1 with and without the series is identical to the digit — 5.98 h paired, 72,768 two-sided rows, 27 events at 2σ / 14 at 3σ — because everything retained today is still in the live log. (3) On-change compaction ALONE would have silently deleted samples: q1 treats a quote older than `QUOTE_STALENESS_MS = 15 s` as MISSING, and an unchanged book emits nothing, so a live quote could read as five minutes old. The 10 s heartbeat is what makes "runs unchanged" true, the two constants now cross-reference each other in prose, and a test imports both and asserts the inequality. (4) The window can only grow forwards, and that is measured, not assumed: `kalshi-crypto-decisions.jsonl` (10 days) carries a `yes_bid`/`yes_ask` pair for these markets, but over 1,977 overlapping rows only 46 (2.3%) match the recorded top of book — its `yes_bid` is systematically `ask − 0.005`, the MIDPOINT — and `kalshi-venue-features.jsonl` (16 days) fails the same way (4.2% / 4.9% agreement; a midpoint and its complement). Backfilling either would have fabricated a book, so the only honest backfill is the 6.0 h the rotations still held. Suite 147/147; ctest NAME set = main's 146 ∪ exactly `{test_kalshi_lag_series}`, none dropped. ELEVEN neuters, feature COMMITTED first, each turning exactly its own tests red: heartbeat, sidedness, time-pruning, reader injection (4 tests including the q1 end-to-end), gap detection, cursor, band/expiry filter, manifest span, lost-state recovery (2), newer-schema refusal, on-change selection (5, including the price-path proof). Surprises: (a) the fifth criterion — "a materially larger event count" — cannot be met on the day it ships and no honest trick makes it, because at the first compaction the retained quote span (5.98 h) was NARROWER than the 8.2 h the report measured: a ticker rotation landed at 22:38Z and took the older half with it. The mechanism is proved by test instead (a retained series older than the live log widens q1's paired window 0.65 h → 2.97 h and its 3σ events 1 → 8, q1 unedited), and the report's Q1 table is deliberately NOT regenerated — regenerating on a narrower window would be worse than not regenerating. (b) The compactor is cheap enough to be boring: 2.8 s for the 201,492-row backfill and 1.0 s for an incremental run (measured with `time`, not eyeballed — the incremental cost is re-scanning the ~80 MB of source rotations for rows past the cursor, not the rows kept, and it is spent once per 900 s interval), ~66 MB/day retained (BRTI 20, quotes 46) against a source pair spending 322 MB/day — a 4.9× reduction that turns a size bound of hours into a time bound of 30 days. (c) A hole had to be made to read as a hole: if this job stops for longer than the ~5.7 h the ticker log survives, the cursor would silently resume later and the series would look continuous across the missing span, so the compactor compares its cursor against the oldest row the source still holds and records the uncovered span in the manifest's `gaps`. (d) `kalshi-tickers.jsonl` rows are one-sided far more often than they are two-sided (72,768 of 153,495 rows are two-sided): the one-sided ones are retained AS one-sided with `book_sided`, never completed from `1 - no_bid`, which is why the venue-features rejection above is the same refusal applied to a different file. (e) The advisor found the one real hole after the PR was open, in the recovery path rather than the happy one: `load_state()` fell back to a zeroed state on a missing or unparseable `state.json`, and since day files are opened for APPEND, a run with reset cursors re-reads the whole source and retains a SECOND copy of everything still on disk — a silent duplication that would have inflated the manifest's counts and q1's row totals with observations that never happened twice. The series already knows where it got to, so recovery now reads the newest retained row per stream back out of the day files (`file_stats` already computed it), and a state file from a NEWER schema is refused with exit 2 rather than overwritten — #145's fail-closed direction, because resetting is precisely the branch that duplicates. The same round added the property the whole 'runs unchanged' claim rests on and which I had only argued in prose: retaining on every `(bid, ask)` transition makes the retained book identical to the raw one at every instant a backward-looking lookup can ask about, asserted over a 30-minute path with churning sizes and 600 raw rows compacted to under half. (f) The advisor's second pass found the defect with a date on it: `parse_threshold_ticker` inherited `kalshi_edge_common.EASTERN_JULY`'s FIXED UTC-4 offset but not its name's caveat, and US DST ends 2026-11-01 — after which `KXBTCD-26NOV0114` computes 18:00 UTC where the true ET close is 19:00 (verified against `zoneinfo`), so the in-band filter would retain the SECOND-to-last hour before expiry and drop the last one, silently, in a job that runs forever and fails exactly when the series is finally long enough to be worth something. Not fixed here: the same offset decides which rows q1 analyses, and a series parsing close times differently from the analysis reading it is worse than one uniformly wrong — both must move together (#176). What this PR owes is disclosure, so `close_time_assumption` now sits in every day-file header and in the manifest, named with its expiry date, and a test asserts the header carries it.
- 2026-07-27 issue #171 PR #177: the calibrator's Brier bookkeeping — schema 2 scores ONE number per resolved contract (mean squared error over that contract's observations, from predictions taken BEFORE the models train on it), reports the raw mid as `brier_market_mid_raw` beside the disambiguated `brier_market_trained_logit`, and gates `adds_value_over_market` on `MIN_SCORED_CONTRACTS = 100` CONTRACTS against the raw mid. Training is untouched — both models still learn from every observation; only the bookkeeping moved. Load-bearing decisions: (1) the autopsy did NOT pick a which-observation rule — #169's `q2` scores row-weighted Brier and reports the contract count only as the effective sample — so the rule was mine to choose and therefore had to be STATED rather than matched; I took the within-contract mean over the first-to-last observations because a first-observation rule would make the score depend on when the daemon happened to start watching, and said in the header that the two estimators will not reproduce each other digit for digit. (2) Scores are read with an explicit `predict()` pass BEFORE the training pass rather than off `update()`'s return value — same three scorers, same rows, and out-of-sample with respect to the contract being scored. (3) `training_observations` is `state['full']['n_seen']`, already in the file and surviving migration intact, so no new counter and no 'trained on 0 observations' hole while the weights say otherwise (advisor's catch). (4) Migration DISCARDS the schema-1 pairs and counts them: the file cannot say which pairs belonged to which contract, so reinterpreting 500 pairs as 500 contracts is the exact defect being fixed. Suite 146/146, `tests/CMakeLists.txt` untouched so the ctest NAME set is identical to main's by construction. Neuters, feature committed first: the 26 python tests run against `origin/main`'s module in a scratch tree gave 13 errors + 1 failure; `signal_trusted` back to `brier_market_baseline` turned 22 decision cases red including the new schema-1 test at its positive control; funnel/cockpit back to `resolved_contracts` turned `tst_kalshi_evidence` and `tst_kalshi_bot_cockpit` red at exactly their new assertions and nothing else. Surprises: (a) `brier_market_baseline` being a TRAINED logit meant the raw mid had never been computed anywhere in the calibrator — the fix adds a measurement, it does not rename one. (b) The operator's live state file holds 1002 resolved contracts and 30,184 training observations behind 500 stored pairs, and a read-only migration preview reports `scored_contracts: 0` off it — so the bot reads the signal as untrusted for days after deploy, which is the fix working and had to be said in the PR before a reviewer read 'bot never bids now' as a regression. (c) `scripts/research/` was the consumer my first repo-wide grep excluded: `q2_calibrator_error_anatomy.py` reads the state file directly (it deliberately never imports the module, because `run_once()` would rewrite the operator's file) and would have `.get`-degraded to zeros against schema 2 — it now branches on `schema`, with the schema-1 path kept verbatim because it is the autopsy's published evidence. (d) `BRIER_WINDOW = 500` and `SCORED_CONTRACT_WINDOW = 500` are the same number in different units: the reported Brier now trails ~500 contracts (weeks) where it used to trail ~8-10 (hours). (e) Labelled `needs-human-review` — the issue handed that judgment to the builder and this changes what `signal_trusted()`, the single gate on whether the bot bids, believes; tightening-only, but the label costs only auto-merge.
- 2026-07-28 issue #172 PR: https://github.com/Trucker2827/Open-Terminal/pull/178 — F3 of the #169 autopsy: the one non-trivial slice where the calibrator beat the mid (high-vol tercile, 0.1151 vs 0.1218 over 116 contracts) does NOT survive, which is the outcome the issue pre-authorised. The pooled-row cell reproduces (−0.0066, 0.1143 vs 0.1209) and then dissolves three ways: it REVERSES under one-contract-one-vote (+0.0021), it sits at paired-by-contract t=0.35 (sign test 66/116, p=0.16), and — the actual finding — the 116 contracts are only THREE volatility episodes, one of which carries 96% of the rows. Episode 2 (07-27 12:27→17:01, 4.6 h) holds the entire effect at −0.0079; episodes 1 and 3 both favour the market (+0.0175, +0.0240). So the honest statement is not "the exception is refuted at some cut point" but "there is no high-volatility exception, there is one high-volatility afternoon" — and that is a retention fact, not a calibrator fact, which sharpens F1/#170's case rather than the calibrator's. Low/mid control reads +0.0112 at t=3.13, so the machinery does report a real effect when one exists; that control is what distinguishes "exception died" from "sign bug in my fold code", and the advisor was right to insist on it. Surprises: (1) The stricter-sounding freeze is the WRONG one. Re-deriving the tercile from an in-time training prefix — which sounds more rigorous than reusing #169's literal — yields 1.81 bps/min and admits 95.9% of later rows, because BTC per-minute vol is strongly non-stationary across this window (07-25/26 max out at 1.62 and 2.36 bps; 07-27 hits 11.31). It would have scored "high vol" on nearly the whole sample while calling it the exception. Ran the diagnostic BEFORE choosing, per the advisor, instead of picking the more virtuous-sounding option and discovering this in review; it ships as `prefix_derived_threshold_diagnostic` rather than being silently dropped. General lesson: a "volatility tercile" over a 3-day window is a fact about the window, not about a regime. (2) Sensitivity came out BETTER than expected and it still doesn't rescue the finding — row-weighted Δ is monotone in the threshold (+0.0042 at 2.45 → −0.0208 at 8.00), not a spike at one cut point, which is genuine weak support for the Q1 mechanism; but every tighter cut is a SUBSET of episode 2, so tightening discards the episodes that disagreed rather than adding evidence, and the t=−2.55 at 8.00 bps is 48 contracts from one afternoon. Monotone-in-the-parameter is not the same as robust when the parameter only ever selects the same event. (3) Nothing is fitted here, so "walk-forward" buys less than the phrase implies: no parameters are estimated, the folds cannot guard against model overfitting, and the pooled figure is BY CONSTRUCTION identical to scoring the whole subset. Said so in the script docstring and the report, because a reviewer checking "walk-forward evaluation" against the criteria would otherwise credit machinery that isn't there. (4) `git checkout -q origin/main -- .` while trying to build a comparison tree reverted my whole working tree to origin/main — commits survived, uncommitted work would not have. Committing before the experiment (the same lesson as #151's neuter) is what made it a non-event; use a `git worktree` for main comparisons, never a checkout into the live tree. (5) Local suite is 143/147: four kalshi bot e2e tests fail at their CONTROL precondition ("control tick placed no bid"), reproduce identically on a pristine origin/main worktree with the same binary, and Linux CI is green on cf37cdbb — the exact commit branched from. Pre-existing macOS/environment artifact, flagged in the PR and left untouched. Worth an operator look: those four are safety-critical bot rungs, and they are honestly refusing to prove anything rather than passing vacuously, which is the right failure mode but not a state to leave sitting.
